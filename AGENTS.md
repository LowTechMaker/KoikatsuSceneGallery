# 專案工作規則

- 開工前先閱讀 `docs/plugin-conventions.md`。
- 保留使用者尚未提交的變更，只修改目前任務需要的內容。
- 修改完成後至少執行相關單元測試與 `KoikatsuSceneGallery.csproj` 建置。
- 朋友功能的已知問題與排查結果記錄在下方；新增問題時必須附上證據、影響、狀態與驗證方式。

# 朋友功能稽核（2026-07-31）

狀態使用「待修復／修復中／已修復／設計決策」；嚴重度使用「高／中／低」。

## 稽核執行紀錄

- 2026-08-26：完成朋友／本人卡片拖放與詳情比例自適應（FRI-071）。朋友清單項目與置頂本人項目可直接接受有效卡片 PNG，只建立連結且不搬移原檔；詳情卡片可用 Copy 資料包拖出。詳情格子改以卡片實際寬高比中位數、縮圖尺寸設定及可用寬度計算統一欄寬／列高。核心測試 166/166、x64 Debug 建置 0 錯誤 0 警告，三個語系資源均可解析且 `git diff --check` 通過；最新 x64 實際畫面確認 YEAsukura 場景卡為橫式四欄、角色卡為直式四欄，圖片完整且未裁切。自動化工具無法可靠完成跨視窗拖放，拖到朋友清單及拖到檔案總管仍保留為人工整合驗證項目。
- 2026-08-01：修復 Fanbox 外掛造成的偶發十秒關閉延遲（FRI-070）。外掛現在於 disposal 開始時取消自己的生命週期權杖，作者查詢使用 caller／shutdown linked token，共享卡片 producer 只連結 shutdown token。Fanbox 離線測試 65/65、核心測試 137/137、x64 Debug 建置通過；五輪已初始化 WebView2 的正常關閉為 164.4～249.0 ms，平均 197.6 ms，沒有新的 disposal timeout、crash log 或朋友／本人資料變更。
- 2026-08-01：完成本人資料夾延遲閘門與朋友實體路徑衝突保護（FRI-010、FRI-066～069）。本人沒有可用資料夾時一定先詢問，按「暫不建立」留在朋友頁；既有 `#Self` 只綁定不改目錄，部分分類可進入且拒絕缺少分類時會還原頁籤。朋友同名實體目錄未綁定時先詢問、已被本人或其他朋友占用時阻止共用。核心測試 137/137、正確 x64 Debug 建置 0 錯誤 0 警告；Windows App SDK 分析器仍只有其他既有頁面的 48 項警告。四組新 UI 流程分別 6/6、4/4、4/4、4/4，修正測試 canonical JSON 後完整朋友流程 21/21。所有受控目錄已清除，`friends.json` 與 `self-profile.json` SHA-256 分別回到 `DE8D248734292EA8B36F61E9FB4B85B02E827079824B44253F99F89F223BD315`、`ED2B095B77FABC6D91049C7BDCA5BEFAF21A2F66F0F5A32E34D9DD2A69486B5C`。
- 2026-08-01：完成可實際進入與管理卡片的本人檔案（FRI-061）。本人項目固定在朋友搜尋與清單上方，使用獨立 `self-profile.json`，可改名稱／本機或作者頭像、設定三類 `#Personal/#Self` 專用資料夾、拖放連結卡片與建立角色版本分類，且服務與介面均沒有刪除本人入口。核心測試 132/132 通過，正確 x64 Debug 建置 0 錯誤、0 警告；Windows App SDK 分析器仍回報其他既有頁面的 48 項警告，但 `FriendsPage.xaml` 與 `FriendDetailPage.xaml` 沒有新增警告。
- 2026-08-01：以正確最新 x64 AppX（PID 70132）執行 `artifacts/self-profile-readonly-ui-tests.ps1`，本人置頂、搜尋隔離、進入詳情、三分類、編輯操作、不可刪除、分類切換及閒置穩定共 9/9 通過；3 秒閒置 CPU 在門檻內且前後畫面 SHA-256 相同。畫面人工檢查沒有截斷、重疊或黑畫面。測試後程序已正常關閉，`friends.json` 仍為「凱特、燕主任、YEAsukura」3 筆且 SHA-256 維持 `DE8D248734292EA8B36F61E9FB4B85B02E827079824B44253F99F89F223BD315`；本人仍名為「我」，三類資料夾與卡片連結皆為 0。
- 2026-08-01：依使用者釐清完成朋友層級角色版本分類（FRI-059）。核心測試增至 126/126，正確 x64 Debug 建置 0 錯誤、0 警告；最新 PID 10840 的實際執行檔已核對為工作區 `win-x64/AppX/KoikatsuSceneGallery.exe`。唯讀 UI 驗證確認舊「新增角色資料夾」入口消失、新「建立版本分類資料夾」按鈕與可存取名稱正確、根目錄 `#old_version` 卡片顯示舊版本徽章，設定頁文字也明確限定為朋友資料夾。未按下建立按鈕，`friends.json` 雜湊及 YEAsukura 既有根目錄狀態均未改變。
- 2026-08-01：最終總檢完成：正確 x64 Debug 主程式建置 0 錯誤、0 警告，核心測試 124/124 通過；兩份朋友 UI PowerShell 腳本通過 AST parser，三個語系 `.resw` 均可解析，`git diff --check` 通過。最後的新啟動樣本持續 `Responding=True`，測試 PID 23172 已透過正常視窗關閉流程結束；關閉後 `friends.json` 仍為真實 3 筆且 SHA-256 不變，`crash.log` 也沒有新增紀錄。
- 2026-08-01：修正大型收藏庫啟動排程後，同一真實設定與 17,069 張場景快取從程序建立到 `FriendsNavItem` 可用為 1.660 秒；後續 10 個每秒樣本全部 `Responding=True`，Working Set 穩定於約 295.6 MB。再次新啟動於 1.604 秒擷取到正常側欄、搜尋與載入環，舊全黑畫面未重現。
- 2026-08-01：正確 x64 Debug 修正版的安全化完整 UI 流程連續兩次 21/21 通過，其中一次明確從真實 `YEAsukura` 詳情頁起跑。兩次都以當次唯一 GUID 新增／進入／刪除測試朋友，結束後 `friends.json` 語意快照與基準相同。
- 2026-08-01：5 輪朋友詳情與三分類切換穩定性流程 18/18 通過，畫面雜湊相同、Working Set 下降 1,116 KB、作者索引仍有資料。修正 CPU 摘要格式後的 1 輪短流程 6/6 通過；5 秒閒置 CPU 增量 0 秒、Working Set 下降 4 KB、畫面雜湊相同。
- 2026-08-01：最終 LocalState 仍只有「凱特、燕主任、YEAsukura」3 筆，空 ID、空名稱、`Codex UI Test*` 與暫時 GUID 殘留皆為 0；`friends.json` SHA-256 仍為 `DE8D248734292EA8B36F61E9FB4B85B02E827079824B44253F99F89F223BD315`。`crash.log` 修改時間仍停在 2026-07-31 23:04:51，本輪沒有新崩潰。
- 2026-08-01：唯讀檢查實際 LocalState `friends.json` 為 3 筆，空 ID／空名稱／`Codex UI Test` 名稱／暫時 GUID `cfa4b274-ab64-48b8-9863-806ae6f7eaf9` 均為 0；目前名稱為「凱特、燕主任、YEAsukura」，SHA-256 為 `DE8D248734292EA8B36F61E9FB4B85B02E827079824B44253F99F89F223BD315`。這是測試後的真實新狀態，不得以舊三筆基準覆蓋。
- 2026-08-01：朋友詳情的資料夾按鈕改為核對實體目錄；同路徑重建會送出單一 `Folders` 事件同步標頭。最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。
- 2026-08-01：三分類新增 `CardsChanged` 集合事件；朋友詳情只合併目前分類的 watcher 變更，三分類空白狀態也統一通知。最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。
- 2026-08-01：卡片 watcher debounce 改為依 flush 當下的檔案最終狀態合併事件，解析重試耗盡會移除舊物件；最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。
- 2026-08-01：設定來源事件已收斂為分類明確的單一協調入口，三個主收藏庫改用 `HasCompletedLoad`，朋友／手動連結刪除採保守三分類失效；最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。
- 2026-08-01：同路徑新版卡片的縮圖請求與場景快取提交已加入快照隔離；場景掃描、縮圖與 metadata 快取鍵納入檔案大小。最新 Debug x64 主程式建置 0 錯誤、0 警告，核心測試增至 124/124 通過。
- 2026-08-01：設定來源路徑與合併警告重設已改為可回復的儲存流程；最新 Debug x64 主程式建置 0 錯誤、0 警告，朋友核心測試 123/123 通過，三個語系資源可解析，`git diff --check` 通過。設定儲存故障注入與 UI 錯誤提示仍待整合驗證。
- 2026-07-31：朋友功能核心單元測試共 123 項通過；最新 Debug x64 主程式建置為 0 錯誤、0 警告。測試已覆蓋朋友記憶體交易的失敗回復／成功保留，以及有效來源簽章忽略巢狀根目錄與根目錄內連結、保留外部連結。
- 2026-07-31：完整 UI 自動化流程成功建立並刪除暫時朋友，最後回到朋友清單；畫面只保留測試前既有的「凱特、test、YEAsukura」，檔案管理警告也已還原。
- 2026-07-31：在朋友清單閒置 5 秒，程序 CPU 增量為 0 秒、Working Set 增量為 0 KB；在 `YEAsukura` 詳情頁閒置 5 秒，CPU 增量為 0.0156 秒、Working Set 增量為 12 KB，程序持續回報 `Responding=True`。目前未再觀察到原本的持續刷新或閒置卡頓，仍需完成重複導覽與分類切換測試後才能結案。
- 2026-07-31：本輪 UI 測試沒有新增 `crash.log` 紀錄；`plugins.log` 尾端只有外掛正常載入與 Fanbox 閒置關閉。`friends.json` 解析後仍為 3 筆既有朋友，空 ID、空名稱與 `Codex UI Test` 殘留筆數都是 0。
- 2026-07-31：以最新 Debug x64 建置執行 `artifacts/friend-feature-ui-tests.ps1`，新增朋友、設定路徑介面、詳情頁、三分類、頭像選單、刪除、角色搜尋設定、檔案警告與合併重設共 27 項全數通過，失敗 0 項。
- 2026-07-31：完整朋友流程結束後再開啟作者頁，UI Automation 仍讀到 3 個作者來源分頁與多張可見作者卡；第一張可見卡的場景／角色／服裝統計為 360／20／50，確認朋友功能沒有再次清空作者索引。
- 2026-07-31：以最新建置執行 `artifacts/friend-refresh-stability-tests.ps1`，5 輪進出詳情與每輪三分類切換共 18 項全數通過。最後 5 秒閒置樣本 CPU 增量為 0 秒、Working Set 下降 136 KB，前後畫面 SHA-256 相同，且作者索引仍有資料。
- 2026-07-31：在最新建置的 `YEAsukura` 場景分類實際按下「開啟資料夾」，確認 Windows 檔案總管新開視窗的位置為 `.../Studio/scene/#Personal/YEAsukura`；測試只關閉該次新開的視窗。
- 2026-07-31：測試基礎設施排查發現，指定 `Platform=x64` 的實際輸出位於 `bin/x64/Debug/...`，舊啟動命令仍指向 `bin/Debug/...`。先前 UI 結果只保留為既有功能回歸證據；FRI-017、FRI-018 與 FRI-020 必須從正確 x64 輸出重新完整驗證後才算最終結果。
- 2026-07-31：從正確的 `bin/x64/Debug/...` 啟動最新建置後，`FriendAuthorAvatarList` 的可見 ListItem 已直接使用作者名稱；八個可見選項各自具有不重複的 `FriendAuthor_<16 位雜湊>` AutomationId。
- 2026-07-31：以正確的 `bin/x64/Debug/...` 最新建置從 `YEAsukura` 詳情頁直接再次呼叫已選取的 `FriendsNavItem`，UI Automation 在 24 ms 內找到 `FriendsAddButton`，確認會返回朋友清單。
- 2026-07-31：安全化完整 UI 流程從 `YEAsukura` 詳情頁起跑，在確認新增唯一名稱的暫時朋友後，立即讀取 `friends.json` 時找不到該名稱；腳本依 fail-fast 規則停在第 9 項，沒有進入任何刪除步驟。測試前備份為 `artifacts/friends-ui-test-backup-20260731051833213.json`，基準 SHA-256 為 `1944AEE8AD7BE2A7B3AA74D99FA22AEF596EDA2E03740FC3FA13EF8E7E80927D`。

## FRI-001：所有作者索引變動都被轉送成朋友資料變動

- 嚴重度：高
- 狀態：已修復
- 證據：`FriendService` 同時訂閱 `AuthorInfoService.AuthorsChanged` 與 `AuthorProfilesChanged`，並在任何事件發生時直接觸發 `FriendsChanged`。
- 影響：背景掃描每加入卡片、作者外掛陸續補回名稱或頭像時，即使沒有任何朋友資料改變，朋友頁與詳情頁仍會收到更新。
- 修正方向：卡片數量變動不再通知朋友功能；作者資料事件攜帶 `AuthorKey`，只通知實際連結該作者頭像的朋友。
- 驗證：觸發無關作者更新時，朋友清單與詳情頁的更新計數不得增加；連結中的作者頭像更新時，只更新對應朋友。

## FRI-002：朋友清單對任何變動都整批清空重建

- 嚴重度：中
- 狀態：已修復
- 證據：`FriendsViewModel.Refresh()` 每次都先 `Friends.Clear()`，再逐項建立新的 `FriendListItemViewModel`。
- 影響：任何高頻事件都會讓 `ListView` 容器、頭像與焦點重建，造成肉眼可見的閃爍、刷新及不必要配置。
- 修正方向：朋友事件攜帶朋友 ID 與變動種類；一般頭像或資料夾變動只替換單一項目，只有新增、刪除、重新命名或搜尋才重建排序後的清單。
- 驗證：單一朋友頭像或資料夾變更時，未受影響的清單項目物件與順序保持不變。

## FRI-003：朋友詳情頁監聽所有三種收藏庫重載

- 嚴重度：中
- 狀態：已修復
- 證據：`FriendDetailPage` 對場景、角色、服裝三個 ViewModel 的 `CardsReloaded` 都綁定同一個無條件 `RefreshCards()`。
- 影響：目前顯示角色卡時，場景卡或服裝卡的背景載入完成也會清空並重建目前頁面。
- 修正方向：為三種來源使用分類明確的處理器，只在完成重載的分類等於目前分類時更新。
- 驗證：停留在角色卡頁籤時，觸發場景卡或服裝卡重載不得重建 `Cards`。

## FRI-004：已排入 Dispatcher 的更新可能在離開詳情頁後繼續執行

- 嚴重度：低
- 狀態：已修復
- 證據：`OnFriendsChanged()` 與 `OnCardsReloaded()` 直接排入 `DispatcherQueue`；解除事件訂閱不會取消已排入佇列的委派。
- 影響：快速離開詳情頁時，隱藏頁面仍可能多執行一次頭像解碼或卡片清單重建。
- 修正方向：Dispatcher 委派執行前再次確認頁面仍在訂閱狀態、朋友 ID 與卡片分類仍相符。
- 驗證：事件排入後立即離開頁面，委派不得更新隱藏頁面的集合或標頭。

## FRI-005：詳情頁的本機操作會直接更新一次，再被事件更新第二次

- 嚴重度：中
- 狀態：已修復
- 證據：設定資料夾、拖放卡片、移除卡片與頭像等處理器在呼叫 `FriendService` 後直接執行 `UpdateHeader()` 或 `RefreshCards()`；同一服務方法又會觸發 `FriendChanged`。
- 影響：一次操作會重建兩次標頭或卡片集合，放大閃爍與縮圖請求，也會讓刷新問題較難追蹤。
- 修正方向：所有朋友資料變動統一由型別化事件更新 UI；事件已在 UI 執行緒時直接套用，操作處理器不再重複更新。
- 驗證：每個服務操作只造成一次對應的標頭或卡片集合更新。

## FRI-006：專用資料夾來源的設定、清除與刪除沒有完整失效收藏庫

- 嚴重度：高
- 狀態：已修復
- 證據：`SetFolderAsync()` 與 `ClearFolderAsync()` 只送出朋友資料事件；`DeleteAsync()` 也只有存在手動卡片連結時才送出收藏庫來源事件。
- 影響：重新連結既有資料夾後，其中的卡片不會立即出現；清除資料夾或刪除朋友後，卡片與檔案監看器仍可能留在收藏庫直到下次完整重載。
- 修正方向：設定與清除資料夾時依目前分類精準觸發 `LibrarySourcesChanged`；刪除朋友時合併其資料夾與手動連結實際涵蓋的分類。
- 驗證：設定、清除或刪除各分類資料夾後，只重載受影響的收藏庫，結果與檔案監看來源立即一致。

## FRI-007：設定根目錄與其下的朋友目錄會被重複掃描及監看

- 嚴重度：中
- 狀態：已修復
- 證據：三個收藏庫把設定根目錄與 `#Personal/朋友` 目錄串接後只做字串 `Distinct`；掃描與 `FileSystemWatcher` 都會遞迴子目錄。
- 影響：朋友資料夾通常本來就在設定根目錄之下，同一批卡片會被列舉兩次並建立重疊的監看器，增加啟動與檔案事件成本。
- 修正方向：正規化來源路徑後移除已被另一個根目錄涵蓋的子路徑，同時保留不在目前設定根目錄下的舊朋友資料夾。
- 驗證：根目錄與其子目錄同時輸入時只留下根目錄；互不包含的路徑都保留。

## FRI-008：單筆損壞的舊朋友資料可能讓整份清單載入失敗

- 嚴重度：高
- 狀態：已修復
- 證據：`LoadAsync()` 先清空集合，再在同一個外層 `try` 中對每筆舊 `FolderPath` 呼叫 `Path.GetFullPath()`；任何無效路徑都會跳到外層例外處理。
- 影響：只有一筆路徑格式錯誤時，介面仍會顯示成所有朋友消失，但 `friends.json` 還在，容易被誤認為資料遭刪除。
- 修正方向：逐筆正規化名稱、ID、資料夾與卡片路徑；無效欄位只清除該欄位並記錄日誌，不中止其他朋友載入。
- 驗證：混合有效與無效欄位的資料仍能載入所有可用朋友，修復後的內容會安全存回。

## FRI-009：頭像只檢查副檔名，無效內容也會被接受

- 嚴重度：中
- 狀態：部分修復；容量／像素尺寸上限待決策
- 證據：`FriendAvatarStorage.ImportAsync()` 僅檢查 `.bmp/.jpeg/.jpg/.png/.webp`，現有測試甚至以四個任意位元組驗證匯入成功。
- 影響：使用者可能收到「頭像已更新」，實際上卻無法解碼而顯示空白；任意大型或偽裝檔也會被複製到應用程式資料。
- 已完成：核心儲存會核對 PNG/JPEG/BMP/WebP 簽章，UI 會先用 `BitmapDecoder` 驗證可解碼且尺寸非零；副檔名正確但內容無效的測試已改為必須拒絕。
- 尚未完成：目前沒有檔案容量或像素尺寸上限；超大型但格式有效的圖片仍會被完整複製。需決定合理上限及超限時的本地化提示後才能結案。
- 驗證：除現有無效內容測試外，加入剛好低於上限與超過容量／像素上限的測試。

## FRI-010：不同朋友名稱可能對應到同一個實體資料夾

- 嚴重度：高
- 狀態：已修復
- 證據：資料夾名稱會把 Windows 非法字元替換為 `_`；例如不同顯示名稱可能得到相同的 `#Personal/安全名稱`。此外 `AddAsync()` 與 `RenameAsync()` 未檢查重複名稱，兩筆不同 GUID、相同名稱的朋友也必然取得完全相同的場景、角色與服裝路徑。
- 影響：兩位朋友會互相看見同一資料夾內的卡片；任一朋友建立的角色子目錄也會出現在另一位朋友，破壞私人分類邊界。
- 修正：建立或設定前先以清理後名稱尋找實際目錄，且大小寫不敏感。未綁定的既有目錄顯示實際路徑並詢問是否綁定；若同一路徑已出現在本人或任一朋友的三分類欄位，UI 會顯示目前擁有者並停止，服務層也在同一把交易鎖內再次防守。允許重複顯示名稱，但不能在未確認下共用實體目錄；需要第二份資料夾時必須改名或選擇不同設定根目錄。
- 驗證：核心測試覆蓋大小寫及非法字元清理後的實體名稱；最新 x64 UI 以 `YEAsukura`／`YEASUKURA` 驗證三類資料夾都顯示占用者並阻止新增，4/4 通過且 `friends.json` 雜湊不變。另一個唯一空目錄的受控流程確認未綁定時顯示確認、取消後不建立朋友或其他分類目錄，4/4 通過並已移除測試目錄。

## FRI-011：每個收藏庫都會重新讀取所有手動連結卡片來判斷類型

- 嚴重度：中
- 狀態：已修復
- 證據：場景、角色、服裝收藏庫各自取得同一份 `GetLinkedCardPaths()`，再對每個檔案執行會開檔的 `CardTypeClassifier.Classify()`。
- 影響：手動連結增加後，一次三分類重載最多會重複開啟並分類同一批檔案三次。
- 修正方向：在朋友服務維護帶檔案時間戳的分類快取，或把卡片類型持久化並提供分類後的查詢。
- 驗證：未變更的手動連結在跨分類重載期間只需分類一次。

## FRI-012：朋友卡片格缺少逐項可存取名稱

- 嚴重度：低
- 狀態：已修復
- 證據：`FriendCardsGrid_ContainerContentChanging()` 只請求縮圖，沒有像朋友清單一樣替容器設定名稱與穩定 AutomationId。
- 影響：螢幕閱讀器難以辨認卡片，UI 自動測試也只能依賴重複的內層按鈕 ID。
- 修正方向：以檔名設定容器可存取名稱，並以可重現的路徑雜湊建立逐項 AutomationId。
- 驗證：每張可見卡片容器都有名稱且 AutomationId 不重複。

## FRI-013：朋友操作失敗時通常只寫日誌，介面沒有錯誤回饋

- 嚴重度：中
- 狀態：程式修復完成，待正確 x64 UI 驗證
- 證據：`UiEventGuard` 會吞下例外並只呼叫 `LogError`；朋友新增、重新命名、儲存及資料夾建立多數沒有自己的失敗提示。
- 影響：路徑無效、權限不足或儲存失敗時，按鈕看似沒有反應，與先前「開啟資料夾沒有回應」的體驗相同。
- 修正：`UiEventGuard` 新增可接收例外的 UI 回呼；朋友清單加入操作狀態 `InfoBar`，清單與詳情頁的朋友操作都會在保留錯誤日誌的同時顯示本地化失敗訊息。開啟資料夾與頭像驗證仍保留更精確的既有提示。
- 靜態驗證：三個語系的 `.resw` 均可解析，最新 Debug x64 建置為 0 錯誤、0 警告。
- 待驗證：在正確 x64 UI 中注入或觸發可安全重現的操作失敗，確認 `InfoBar` 可見、可關閉且不會清空既有資料。

## FRI-014：朋友資料會先改記憶體、再嘗試寫入磁碟

- 嚴重度：高
- 狀態：成功路徑已完成正確 x64 UI 驗證；失敗提示整合驗證待補
- 證據：新增、重新命名、刪除、設定頭像、資料夾與卡片連結都先修改 `FriendRecord` 或 `Friends`，之後才呼叫 `SaveAsync()`，失敗時沒有回復。
- 影響：磁碟滿、權限或暫存檔錯誤時，記憶體、畫面事件與 `friends.json` 可能處於三種不同狀態，重新啟動後又會回復舊資料。
- 修正：所有朋友寫入操作現在共用同一把非同步交易鎖與 `FriendMutationTransaction` 快照；`SaveAsync()` 失敗時會回復原物件欄位、集合順序與物件身分，且不送出成功事件。失敗操作新匯入的受管理頭像與新建立的空資料夾會清理；舊頭像則只在資料成功持久化後刪除。事件訂閱者及成功後清理的例外會各自記錄，不再反向破壞已提交操作。
- 自動驗證：新增故障注入測試確認失敗時完整回復原物件、欄位與順序，成功時保留變更；朋友功能核心測試 121/121 通過，最新 Debug x64 建置 0 錯誤、0 警告。
- UI 驗證：2026-08-01 安全化完整流程兩次 21/21 通過，每次都成功持久化新增朋友、以解析出的唯一 GUID 操作、再持久化刪除，最後 `friends.json` 語意快照與基準相同。仍待在 UI 安全注入儲存失敗，確認 `InfoBar`、記憶體回復與磁碟檔一致。

## FRI-015：任何祖先目錄叫做保留名稱都會被判定成角色版本

- 嚴重度：中
- 狀態：已修復
- 證據：`ClassifyCharacterPath()` 會一路向上尋找 `#old_version` 或 `#alternatives`，但沒有確認它是否位於受支援的角色結構內。
- 影響：一般收藏庫若剛好含有同名資料夾，甚至設定根目錄的祖先同名，卡片也會被隱藏成舊版或替代版。
- 修正方向：只有保留目錄的父層能被辨認為 `Characters/角色` 或 `#Personal/朋友/角色` 結構時才分類。
- 驗證：相同保留名稱在結構內會分類，在一般任意路徑中則維持目前版本。

## FRI-016：場景快取用字串前綴判斷是否位於來源根目錄

- 嚴重度：低
- 狀態：已修復
- 證據：`GalleryViewModel` 以 `FilePath.StartsWith(root)` 篩選快取；根目錄 `C:\cards` 也會錯誤匹配 `C:\cards-old`。
- 影響：朋友來源切換或程式啟動時，來源外的快取卡片可能短暫出現在畫面，直到實際掃描完成才被移除。
- 修正方向：改用路徑區段感知的 `FriendFolderLayout.IsWithin()`。
- 驗證：相同字首但不是子路徑的卡片不會進入相關快取。

## FRI-017：被取消的舊收藏庫載入仍會宣告卡片重載完成

- 嚴重度：中
- 狀態：已修復
- 證據：場景、角色與服裝 ViewModel 的 `finally` 只用目前 token 判斷是否清除 `IsLoading`，但不論該 token 是否已被新載入取代，都會無條件呼叫 `RaiseCardsReloaded()`。
- 影響：來源快速連續失效時，舊載入被取消後仍會要求朋友詳情頁重建目前分類；此時集合可能剛被新載入清空或尚未完成，因此會多一次空白閃爍。
- 修正方向：只有仍是目前最新 token、而且正常跑完的載入能清除 `IsLoading` 並送出 `CardsReloaded`；被取代或取消的載入安靜結束。
- 驗證：連續啟動兩輪同分類載入時，舊 token 取消不送完成事件，只有正常完成的最新一輪送出一次。

## FRI-018：空分類會被誤判成從未載入

- 嚴重度：中
- 狀態：已修復
- 證據：`FriendDetailPage.EnsureSelectedCategoryLoaded()` 使用三個收藏庫的 `Cards.Count == 0` 判斷是否需要載入，沒有區分「尚未載入」與「已成功載入但結果為空」。
- 影響：合法的空分類每次重新進入朋友詳情頁或切回該分類，都會再次掃描整個設定根目錄與朋友來源，造成不必要的磁碟工作與載入動畫。
- 修正方向：收藏庫明確保存「最新載入已成功完成」狀態；朋友詳情頁只在尚未成功載入且目前沒有載入中的情況下啟動載入。
- 驗證：空分類成功載入一次後，重複切換頁籤不會再啟動掃描；被取消或失敗的載入仍允許稍後重試。

## FRI-019：開啟朋友資料夾失敗時看起來完全沒有反應

- 嚴重度：中
- 狀態：已修復
- 證據：先前使用者按下「開啟資料夾」後沒有開啟視窗，介面也沒有失敗訊息；一般事件保護只會把例外寫入日誌。
- 影響：使用者無法分辨是按鈕未觸發、路徑不存在、Windows 拒絕啟動，或程式仍在處理。
- 修正方向：先以 `StorageFolder.GetFolderFromPathAsync()` 取得明確資料夾，再檢查 `Launcher.LaunchFolderAsync()` 回傳值；路徑遺失、回傳失敗與例外都顯示本地化提示。
- 驗證：實際按鈕測試必須新開指向目前分類路徑的檔案總管視窗；不存在或無法開啟時顯示警告。

## FRI-020：已知作者頭像選項沒有可辨識的自動化名稱

- 嚴重度：低
- 狀態：已修復
- 證據：實際檢查 `FriendAuthorAvatarList` 時，所有可見 `ListItem` 的 UI Automation 名稱都是 `KoikatsuSceneGallery.Models.AuthorDisplay`，只有內層文字才顯示作者名稱。
- 影響：螢幕閱讀器無法直接宣告每個選項是哪位作者，UI 自動測試也無法用作者名稱穩定選取項目。
- 修正方向：容器建立時以作者顯示名稱設定 `AutomationProperties.Name`，並以作者 provider 與 ID 的雜湊設定穩定 `AutomationId`。
- 驗證：每個可見作者選項的 ListItem 名稱等於畫面作者名稱，AutomationId 不重複且重新開啟對話框後保持相同。
## FRI-021：UI 自動測試在前置導覽失敗後仍繼續執行刪除（嚴重）

狀態：已修復

關鍵發現：`artifacts/friend-feature-ui-tests.ps1` 假設呼叫已選取的「朋友」導覽項目一定會回到朋友列表；若測試從朋友詳情頁開始，呼叫不會切頁。前置檢查雖然失敗，現有 `Test-UI` 仍會繼續後續步驟，最後把當前真實朋友誤當成測試資料並執行刪除。2026-07-31 的正確 x64 驗證中，`YEAsukura` 因此從 `friends.json` 被移除，朋友記錄由 3 筆變成 2 筆。

影響：自動測試可能刪除使用者的真實朋友資料，屬於資料完整性問題；在修復並驗證前，禁止重跑會建立或刪除朋友的 UI 測試。

修正：兩份朋友 UI 腳本都先切到作者頁再進朋友列表，避免已選取導覽項目不觸發導航；所有測試改為首個失敗即中止。完整流程現在要求傳入 `friends.json`，執行前先備份，新增後從資料檔解析唯一測試 GUID，後續以 `FriendItem_{Guid:N}` 操作，進詳情與刪除前同時核對儲存記錄及 `FriendNameText`。清理遇到名稱不唯一、GUID 不符或任何 UI 前置失敗時會保留測試資料並報錯，不再猜測刪除目標；穩定性腳本也以資料檔解析的精確 GUID 開啟朋友。

驗證：2026-08-01 正確 x64 修正版完整流程連續兩次 21/21 通過；第二次明確從真實 `YEAsukura` 詳情頁起跑，腳本先跨到作者頁再回朋友列表。兩次都只刪除當次新增且已驗證 GUID 的測試朋友，前後語意快照相同，真實三筆資料與 SHA-256 不變。兩份 PowerShell 腳本最終均通過 AST parser，零語法錯誤。

復原進度：已完成。原始朋友 GUID `9cb6d69c-5c4a-4540-8984-b18c01998ad6`、名稱、三類專用資料夾路徑、`AvatarPath`、provider `pixiv`、作者 ID `60114281` 及空 `CardPaths` 均已找回。測試前截圖的圓形頭像已與 Pixiv 作者快取 `60114281.jpg` 做像素／構圖核對，人物姿勢、手部、眼鏡與裁切一致；`AvatarPath` 則依 `ThumbnailCacheService` 的路徑、修改時間 ticks 與 SHA-256 規則重算為既存的 `gallery_temp\4081507B45BDB46C.jpg`。寫回前已逐欄確認「凱特、test」兩筆完全不變，並保留 `artifacts/friends.after-test-incident-20260731.json` 與 LocalState 原子替換前備份；原子替換後 `friends.json` 為 3 筆、ID 唯一且頭像檔存在。

## FRI-022：作者來源協調器仍把空集合判定成尚未載入

- 嚴重度：中
- 狀態：程式修復完成，待正確 x64 UI 驗證
- 證據：`FriendDetailPage` 已使用三個收藏庫的 `HasCompletedLoad`，但 `AuthorSourceCoordinator.AwaitOrLoad()` 仍以 `cardCount > 0` 決定是否略過載入；成功完成且合法為空的來源仍傳入 `0`。
- 影響：每次從朋友頁回到作者頁並呼叫 `EnsureLoadedAsync()` 時，空的場景、角色或服裝來源會再次掃描磁碟，造成不必要的卡頓，並可能被誤認為頁面持續刷新。
- 修正：協調器已改為傳入並檢查 `HasCompletedLoad`，只有從未成功完成或明確要求重載時才執行命令。
- 靜態驗證：最新 Debug x64 建置為 0 錯誤、0 警告。
- 待驗證：空來源完成一次後，重複進入作者頁不再執行載入；失敗、取消及 `forceReload` 仍可重試。

## FRI-023：在朋友詳情頁再次按左側「朋友」不會返回清單

- 嚴重度：低
- 狀態：已修復
- 證據：朋友詳情頁沿用已選取的 `FriendsNavItem`；`MainWindow` 只處理 `NavigationView.SelectionChanged`，再次呼叫已選項目不會產生選取變更，因此 `QueueNavigation(typeof(FriendsPage))` 不會執行。這也是 FRI-021 測試前置導覽失敗的直接條件。
- 影響：使用者在朋友子頁按父層導覽項目看似沒有反應，只能使用返回鍵；自動化若錯把按下動作等同成功導航，也容易在錯誤頁面繼續。
- 修正：`NavigationView.ItemInvoked` 現在會在目前頁面為 `FriendDetailPage` 且呼叫朋友項目時明確導航回 `FriendsPage`；其他一般選取仍維持原本去抖動流程。
- 靜態驗證：最新 Debug x64 建置為 0 錯誤、0 警告。
- UI 驗證：從正確 x64 最新建置的 `YEAsukura` 詳情頁直接再次呼叫 `FriendsNavItem`，24 ms 內回到含 `FriendsAddButton` 的朋友清單。完整流程仍會從詳情頁起始狀態再次覆蓋此情境。

## FRI-024：重建相同路徑的遺失資料夾不會使收藏庫來源失效

- 嚴重度：中
- 狀態：程式修復完成，待正確 x64 整合驗證
- 證據：`SetFolderAsync()` 先呼叫 `CreatePersonalFolder()`，接著只要新路徑字串等於記錄中的舊路徑就直接返回。若舊資料夾已被刪除，呼叫確實會重新建立目錄，但不送出 `LibrarySourcesChanged`。
- 影響：啟動時因路徑不存在而未建立的掃描／監看來源，在使用者重新建立同一路徑後仍不會加入；放入的新卡片可能直到下一次完整重載或重啟才出現。
- 修正：`FriendFolderLayout.GetPersonalFolderPath()` 可在不建立目錄的情況下先算出目標；`SetFolderAsync()` 會記錄建立前是否存在。相同路徑若是本次重新建立，仍精準觸發該分類來源重載，但不製造虛假的朋友欄位變更。
- 自動驗證：新增 `GetPersonalFolderPath()` 不建立目錄的測試；朋友功能核心測試 121/121 通過，最新 Debug x64 建置 0 錯誤、0 警告。
- 待驗證：記錄保留、實體目錄刪除的情況下，重新設定相同根目錄會建立資料夾並送出一次對應來源事件；目錄原本已存在時維持無事件的冪等行為。

## FRI-025：設定根目錄外的手動連結卡片沒有檔案監看

- 嚴重度：中
- 狀態：設計決策
- 證據：三種收藏庫載入時會額外讀取 `GetLinkedCardPaths()`，但 `StartWatching()` 只收到折疊後的設定／朋友資料夾根目錄。位於這些根目錄外的單獨連結檔案沒有對應 `FileSystemWatcher`。
- 影響：朋友給予的卡片若在原地被覆寫成新版本、刪除或替換，執行中的收藏庫與朋友詳情不一定立即更新；必須等其他來源事件、手動重載或重啟。
- 設計取捨：直接監看每個連結檔案的父目錄可能建立大量 watcher，而且不能把父目錄整批掃描進收藏庫。可選方向是共用父目錄 watcher 並只接受已連結路徑、以低頻檔案簽章輪詢，或明確維持「重載時更新」並在 UI 提供重新整理。
- 驗證：選定方案後，根目錄外的已連結檔案在原地覆寫、刪除及重建時，只有該卡片與相應分類更新，不匯入同資料夾的其他卡片。

## FRI-026：整份朋友或本人資料損壞時仍會顯示成資料消失

- 嚴重度：高
- 狀態：設計決策
- 證據：FRI-008 只處理成功反序列化後的逐筆修復；`FriendStore.LoadAsync()` 若遇到截斷或 JSON 語法錯誤會直接拋出，`FriendService.LoadAsync()` 外層只記錄日誌，而 `Friends` 維持空集合。新增的 `SelfProfileStore.LoadAsync()` 也採相同模式；本人檔案損壞時會記錄錯誤並顯示預設本人，之後若使用者編輯資料，現行儲存可能覆蓋損壞主檔。兩種 Store 都以暫存檔替換主檔，但不保留上一版。
- 影響：外部編輯、同步工具或儲存媒體錯誤造成整檔損壞時，朋友畫面會像沒有任何朋友，本人則會像回到預設值；使用者沒有錯誤提示，也沒有內建可回復副本。
- 設計取捨：可在每次原子替換時保留一份 `.bak`，主檔無法解析時顯示復原對話框；較保守的方式是只提供「開啟資料位置／從備份復原」而不自動覆寫。自動復原前必須保存損壞檔，避免把仍可人工修復的新資料蓋掉。朋友與本人檔案應採同一決策。
- 驗證：分別以截斷的 `friends.json`／`self-profile.json`、有效備份與主檔備份皆損壞測試；任何情況都不得靜默顯示空白／預設資料，或未經確認覆寫唯一副本。

## FRI-027：新增朋友後，UI 測試立即讀取資料檔時找不到新記錄

- 嚴重度：中（測試基礎設施）
- 狀態：已修復
- 證據：正確 x64 最新建置中，新增對話框、欄位輸入及確認按鈕操作都成功；下一步以唯一名稱 `Codex UI Test 20260731051833125` 讀取 `friends.json` 時卻得到 0 筆。安全腳本立即停止，尚未嘗試開啟詳情或刪除任何資料。
- 根因：稍後重讀資料檔時，新朋友已成功持久化為 GUID `cfa4b274-ab64-48b8-9863-806ae6f7eaf9`，總數為 4，且既有三筆仍完整。`winapp ui invoke` 在按鈕的非同步事件處理完成前就返回，腳本把「呼叫按鈕」誤當成「新增與儲存完成」。
- 影響：腳本會誤報產品失敗並留下暫時朋友；若後續程式碼沒有 fail-fast，還可能在未解析精確 GUID 的情況下繼續操作。這次因 FRI-021 的保護已在解析失敗時停止，沒有發生誤刪。
- 修正：確認按鈕後輪詢 `friends.json`，直到唯一名稱出現或逾時；解析 GUID 後再等待精確 `FriendItem_{Guid:N}` UI 項目。逾時時保留資料與備份，不執行猜測式清理。修正後腳本已通過 PowerShell AST parser。
- 驗證：2026-08-01 兩次完整 UI 流程都在按下確認後成功輪詢 `friends.json`、解析當次唯一 GUID，再等待精確 `FriendItem_{Guid:N}`；包含從真實詳情頁起跑的情境。安全清理後語意快照與 SHA-256 均回到基準。
- 暫時資料現況：2026-08-01 唯讀檢查確認 GUID `cfa4b274-ab64-48b8-9863-806ae6f7eaf9` 與所有 `Codex UI Test*` 名稱均已不存在；實際資料為 3 筆且無空 ID／空名稱。使用者其後已把其中一筆改名為「燕主任」，因此後續 UI 測試只能以當次新快照為基準，不得回復 2026-07-31 的舊檔。

## FRI-028：UI 測試曾擷取到全黑前景視窗

- 嚴重度：高
- 狀態：已修復；FRI-059 本輪疑似復現已確認為測試視窗身分不明，見 FRI-060
- 證據：FRI-027 的暫時朋友成功持久化後，`--capture-screen` 先擷取到黑色內容區與中央載入環，稍後再次擷取只剩全黑內容；改用視窗擷取仍為全黑。`winapp ui list-windows` 同時確認該視窗為前景、大小 1440×753，程序 `Responding=True`，且 UI Automation 仍能立即找到精確暫時朋友項目。
- 影響：若不是擷取工具限制，使用者會在新增朋友造成收藏庫來源重載時看到整個介面被遮住甚至無限黑畫面，與「很卡」及頁面像被清空的回報高度相關；UI 元素雖仍存在，視覺上不可操作。
- 校正發現：靜態檢查確認 `MainWindow` 沒有全域載入遮罩或黑色覆蓋層，三個收藏庫的 `ProgressRing` 只位於各頁卡片區；`FriendService.AddAsync()` 也不送 `LibrarySourcesChanged`，FRI-030 已排除新增朋友需要來源重載。`artifacts/fri027-window-capture.png` 的全黑區連應用程式自訂 TitleBar 與側欄都未合成，只留下 Windows 非客戶區邊框／視窗按鈕，較符合 DComp 或擷取失敗，但仍屬推論。
- 新證據：2026-08-01 使用 Computer Use 的 Windows.Graphics.Capture 擷取同一 PID 36364 視窗，仍只有 Windows 非客戶區按鈕與全黑客戶區；因此已排除只有 `winapp ui screenshot` 單一工具出錯。同一時點 UI Automation 可讀到完整視覺樹、`FriendsNavItem` 及 17,069 張場景卡，但對朋友項目送出點擊後頁面並未導覽，與 FRI-056 的主執行緒無回應一致。
- 先前驗證：修正 FRI-056 後的穩定性 `--capture-screen` 曾正常顯示 `YEAsukura` 詳情、頭像與 4 張場景縮圖；全新啟動 PID 23172 也曾在 1.604 秒時正常顯示側欄、三分類、搜尋列與載入環。證據圖為 `artifacts/fri028-fixed-startup-window.png` 與 `artifacts/friend-stability-after.png`，但這只能證明問題不是必現。
- 本輪誤判與校正：FRI-059 第一次 Computer Use 選窗只依共用 AUMID；工具把同一 HWND 同時列在開發封裝與 `E:\KoikatsuSceneGallery-0.1.5-win-x64\KoikatsuSceneGallery.exe` 之下，未能證明黑畫面屬於剛啟動的 PID 62296。關閉該歧義視窗後重新以 `winapp run` 啟動 PID 10840，先用 `Get-Process.Path` 核對為工作區最新 `win-x64\AppX\KoikatsuSceneGallery.exe`，再選取新回傳的唯一視窗 ID 8594924；Windows.Graphics.Capture 正常顯示 17,069 張場景、朋友清單、YEAsukura 詳情、角色卡與設定頁，所有導覽可操作。故 PID 62296 的黑畫面不能作為產品復現證據，FRI-028 維持已修復。

## FRI-029：UI 測試在詳情頁用「清單項目已消失」判定刪除完成

- 嚴重度：中（測試基礎設施）
- 狀態：已修復
- 證據：`Remove-TemporaryFriendSafely()` 在朋友詳情頁確認刪除後，立刻對朋友清單項目的 AutomationId 使用 `wait-for --gone`；該清單項目在詳情頁本來就不屬於目前 UI 樹，因此即使非同步刪除與持久化尚未完成也可能立即通過。
- 影響：腳本後續可能過早讀取 `friends.json`，誤報暫時朋友仍存在；更重要的是，現有「刪除已完成」證據不可靠，無法保證測試後資料已復原。
- 修正：確認刪除後先等待 `FriendsAddButton`，證明已返回朋友清單；接著輪詢 `friends.json` 直到精確 GUID 消失，最後才在清單頁確認精確 AutomationId 不存在。任一步驟逾時都保留備份並停止。修正後腳本已通過 PowerShell AST parser。
- 驗證：2026-08-01 兩次完整 UI 流程在確認刪除後都先等到 `FriendsAddButton`，再輪詢 `friends.json` 直到精確測試 GUID 消失，最後確認清單不含精確 AutomationId。每次結束後基準語意快照與 SHA-256 均相同，沒有測試殘留或真實朋友變動。

## FRI-030：新增朋友建立專用資料夾後沒有更新收藏庫來源

- 嚴重度：無（已排除）
- 狀態：已排除，先前新增的通知將撤回
- 證據：`FriendService.AddAsync()` 會依三個設定根目錄建立 `#Personal/朋友名稱` 並寫入記錄，但成功後只呼叫 `NotifyFriendChanged(...Collection)`；不像 `SetFolderAsync()`，沒有呼叫 `NotifyLibrarySourcesChanged()`。
- 排除理由：新增對話框只能從目前已設定的分類根目錄選擇位置；新資料夾必然是該根目錄的 `#Personal` 子目錄。`CardScanService.StartWatching()` 對設定根目錄使用 `IncludeSubdirectories=true`，即使子目錄在 watcher 啟動後才建立，後續放入的卡片仍會被觀察；初次／下次完整掃描也本來就涵蓋它。
- 結論：新增朋友不應額外觸發整個分類重載。先前稽核中加入的來源通知會撤回，避免製造 FRI-036 的不必要卡頓。

## FRI-031：朋友與卡片項目內的操作按鈕共用 AutomationId

- 嚴重度：低
- 狀態：程式修復完成，待 UI 驗證
- 證據：`FriendsPage.xaml` 每列都使用 `FriendMoreButton`、`FriendRenameMenuItem`、`FriendDeleteMenuItem`；`FriendDetailPage.xaml` 每張卡片也都使用 `FriendCardMoreButton` 與 `FriendRemoveCardMenuItem`。
- 影響：同一畫面存在多個相同 AutomationId，輔助技術與 UI 自動化無法可靠指定某位朋友或某張卡片；測試容易操作第一個命中項目，增加錯誤目標風險。
- 修正：朋友的更多／重新命名／刪除操作 ID 已加入朋友 GUID；卡片的更多／移除操作 ID 已加入既有的路徑 SHA-256 短雜湊，不把完整私人路徑暴露到 UIA。最新 Debug x64 建置為 0 錯誤、0 警告。
- 驗證：同一畫面所有可見操作按鈕與選單項目 AutomationId 均不重複，重新載入後保持穩定。

## FRI-032：不同角色名稱可能靜默共用同一角色資料夾

- 嚴重度：中
- 狀態：已排除（錯誤需求模型已移除）
- 證據：`CreateCharacterStructure()` 只把角色名稱經 `PathSanitizer.SanitizeFolderName()` 轉成資料夾片段，接著直接 `CreateDirectory()`；不同輸入若淨化後相同，例如 `角色:A` 與 `角色?A`，會得到同一個 `角色_A`，同名輸入也會重用既有目錄。
- 影響：原本不同的角色可能混用卡片、`#old_version` 與 `#alternatives`；介面仍顯示建立成功，使用者不會知道實際上合併到既有角色。
- 排除理由：使用者於 FRI-059 釐清版本分類屬於朋友而非單一角色；「輸入角色名稱並建立角色資料夾」的整個流程已移除，因此不再存在角色名稱淨化或角色資料夾撞名。
- 驗證：主程式已無 `CreateCharacterStructure`、角色資料夾名稱輸入框及對應服務入口；版本分類測試改驗證朋友層級結構。

## FRI-033：朋友檔案警告與合併重設在儲存失敗時不回復旗標

- 嚴重度：中
- 狀態：已修復；故障注入 UI 驗證待補
- 證據：`MarkFriendFileWarningShownAsync()` 先把 `FriendFileWarningShown` 設為 `true`，`ResetGuidanceAsync()` 先把兩個提示旗標設為 `false`，之後才呼叫 `SaveConfigAsync()`；例外時沒有回復原值。
- 影響：關閉警告可能在本次執行看似成功、重啟後卻重新出現；重設也可能在畫面上生效但沒有寫入磁碟。雖然 FRI-013 會顯示失敗訊息，記憶體與持久化狀態仍不一致。
- 校正發現：雖然三個操作已加上 `try/catch` 回復旗標，私有 `SaveConfigAsync()` 自己會捕捉並吞掉所有例外，因此外層 `catch` 永遠不會執行；先前的「程式修復完成」結論不成立。
- 修正：`SaveConfigAsync(bool throwOnFailure)` 可讓交易型呼叫端接收寫入例外；匯入說明、朋友檔案警告與合併重設失敗時會回復操作前旗標。設定頁的合併重設改由 `UiEventGuard` 包裝，失敗時顯示三語系 `InfoBar`；`CanResetGuidance` 也會隨兩個旗標重新通知按鈕狀態。朋友頁只在使用者按下關閉按鈕時保存警告狀態，保存失敗會重新開啟原警告並顯示錯誤。
- 驗證：2026-08-01 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 123/123 通過，三語系 `.resw` 可解析。仍需注入設定儲存失敗，確認旗標、命令可用狀態與警告顯示保持操作前狀態；成功時正常持久化。

## FRI-034：作者頭像選項對不可變屬性使用 OneWay 綁定

- 嚴重度：低
- 狀態：已修復
- 證據：重新編譯 `FriendDetailPage.xaml` 時，XAML 編譯器對 `ProfilePicture="{x:Bind AvatarSource, Mode=OneWay}"` 回報 WMC1506；`FriendAuthorAvatarItemViewModel.AvatarSource` 是建立後不變的唯讀屬性，也不提供屬性變更通知。
- 影響：目前值仍可在建立時顯示，但綁定語意錯誤並產生建置警告；未來若誤以為它會動態更新，介面不會收到通知。
- 修正：已改為符合不可變選項模型的 `Mode=OneTime`；作者資料更新後重新開啟選擇器時會建立新項目。
- 驗證：重新編譯 XAML 後 WMC1506 消失，最新 Debug x64 主程式為 0 錯誤、0 警告。

## FRI-035：朋友來源重載失敗會把收藏庫與作者索引留在空白或半成品狀態

- 嚴重度：高
- 狀態：程式修復完成，待取消／故障整合驗證
- 證據：場景、角色與服裝三個 `LoadCardsAsync()` 都在掃描前先執行 `Cards.Clear()` 與索引清除；只捕捉並忽略 `OperationCanceledException`。若設定讀取、快取、目錄掃描、卡片解析或 watcher 啟動拋出其他例外，方法直接失敗；若收藏庫頁離開而呼叫 `CancelPendingWork()`，目前最新載入也會停止。兩者都沒有回復上一份成功資料。
- 影響：朋友資料夾設定、清除、刪除或新增會強制重載受影響收藏庫；其中任一步驟失敗，或使用者在載入中離開收藏庫頁時，收藏庫可能變空或只保留部分批次。`AuthorInfoService` 又會隨集合清除重建計數，造成「作者頁被清空但實體檔案仍在」的現象。這與使用者實際回報一致。
- 修正：三個收藏庫都保存最後一次成功完成的卡片快照；從完整狀態重載前會更新快照，納入 watcher 後續變動。目前最新載入若錯誤或被單獨取消，會回復卡片、路徑索引、角色版本索引與篩選；被更新一輪取代的舊 token 因 token 不符而不得回復。取消時不重新啟動已由離頁流程停止的 metadata 掃描。最新 Debug x64 建置為 0 錯誤、0 警告。
- 驗證：對三種收藏庫分別在清空後、部分批次後注入例外與單獨取消；畫面及作者計數都回復上一份完整資料。由新載入取代的舊 token 取消時不得回復並覆蓋新載入，下一次重試可正常成功。

## FRI-036：朋友變動即使沒有改變有效掃描來源，仍強制整批重載收藏庫

- 嚴重度：高
- 狀態：程式修復完成，待 UI 載入計數驗證
- 證據：`SetFolderAsync()`、`ClearFolderAsync()`、`DeleteAsync()`、`AssignCardsAsync()` 與 `RemoveCardAsync()` 只要朋友的資料夾或連結欄位改變，就送出分類事件；`AuthorSourceCoordinator` 對每個事件都以 `forceReload: true` 執行完整載入。另一方面，`CollapseNestedRoots()` 會移除已被設定根目錄涵蓋的朋友子目錄，設定根 watcher 又遞迴監看；根目錄內的手動連結卡片也早已在收藏庫中。
- 影響：常見的 `設定根/#Personal/朋友` 關聯、清除關聯，或連結／解除連結一張本來就在收藏庫根目錄內的卡片，都會無意義地清空並重掃整個分類，造成卡頓、載入動畫與作者索引暫時消失。多個分類或連續操作會把成本疊加。
- 修正：協調器會在串行佇列輪到每個朋友事件時重新建立三分類有效來源集合：折疊後的掃描根目錄，加上不在任何根目錄內且分類相符的手動連結檔案；只有集合相對最後成功載入實際改變的分類才強制重載。載入完成時也只在來源仍等於起始快照時提交簽章，避免較舊工作覆蓋新設定。
- 自動驗證：新增兩項核心測試確認巢狀朋友根與根內連結不改變來源集合、根外連結會改變；總測試 123/123 通過，最新 Debug x64 建置 0 錯誤、0 警告。
- 驗證：設定／清除根目錄內朋友資料夾、連結／解除根目錄內卡片時，三個載入命令執行次數不增加；外部舊朋友路徑與根目錄外手動連結的新增／移除仍只重載正確分類。

## FRI-037：一次來源載入失敗會阻止後續朋友來源重載

- 嚴重度：高
- 狀態：程式修復完成，待連續故障整合驗證
- 證據：`OnFriendLibrarySourcesChanged()` 把 `_warmupTask` 傳入 `ReloadFriendSourcesAsync()`；後者先直接 `await previousLoad`，沒有處理 fault。只要上一輪載入拋出非取消例外，下一輪會在同一行再次拋出，永遠不執行新的 `LoadAsync()`。
- 影響：暫時性的磁碟、權限或解析錯誤可能使朋友來源更新永久失效；使用者修好路徑、重新建立資料夾或再次操作後，收藏庫仍不會恢復，直到重啟或其他未依賴該 task 的流程介入。
- 修正：串行佇列會等待並觀察上一輪 task，但忽略其已由原始擁有者記錄的 fault；接著重新擷取目前有效來源集合並決定載入。朋友來源這一輪若失敗，協調器會記錄一次並讓 task 正常結束，因此下一輪不再被 fault 鏈阻擋。最新 Debug x64 建置為 0 錯誤、0 警告。
- 驗證：第一輪注入例外、第二輪恢復正常時，第二輪仍確實執行並成功更新；錯誤只記錄一次且不造成未觀察 task 例外。

## FRI-038：設定根目錄變更啟動的來源重載 task 沒有被觀察

- 嚴重度：中
- 狀態：程式修復完成，故障注入驗證待補
- 證據：三種設定路徑事件呼叫 `OnFolderPathsChanged() => Refresh(true)`；`Refresh()` 把 `LoadAsync()` 指派給 `_warmupTask` 後直接返回 `void`，沒有 `await`、`Observe()` 或內部例外處理。
- 影響：設定路徑引發的載入若失敗，可能只留下未觀察 task 例外，缺少可診斷日誌；下一個流程雖會因 FRI-037 修正而繼續，但使用者仍不知道這一輪為何沒有更新。
- 修正：保留 `_warmupTask` 供串行協調，同時用既有 `Observe(IAppLogger, operation)` 觀察背景 task；不重複吞例外或改變後續重試。
- 驗證：2026-07-31 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 123/123 通過；仍需注入設定路徑載入例外，確認日誌恰有一筆具名錯誤且下一次正常變更仍能載入。

## FRI-039：重新建立檔案 watcher 失敗時會先失去全部舊 watcher

- 嚴重度：高
- 狀態：程式修復完成，watcher 故障注入驗證待補
- 證據：`CardScanService.StartWatching()` 一開始就呼叫 `StopWatching()`，再逐路徑建立並啟用新 `FileSystemWatcher`。若某個後續路徑因權限、資源或 I/O 問題拋出例外，舊 watcher 已全部釋放，新組也只完成一部分。
- 影響：FRI-035 即使回復上一份卡片與作者資料，後續在原資料夾新增、刪除或重新命名卡片也可能完全沒有反應；使用者只會在下一次完整掃描看到變化。
- 修正：先在獨立清單建立、設定並啟用完整的新 watcher 組；任一步驟失敗便關閉新組並保留舊組。全部成功後才替換並釋放舊組。短暫重疊事件由既有 `HashSet` 去重。
- 驗證：2026-07-31 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 123/123 通過；仍需讓第二個新路徑的 watcher 啟用失敗，確認舊路徑事件仍正常，並確認成功替換後每個新根只收到一次有效變更。

## FRI-040：設定路徑在載入途中變更時，新來源可能被誤標為已完成

- 嚴重度：高
- 狀態：程式修復完成，競態整合驗證待補
- 證據：`Refresh(reloadLoadedSources: true)` 直接啟動新的 `LoadAsync()`。若分類 ViewModel 正在載入，`AwaitOrLoad()` 只把既有 `ExecutionTask` 加入等待清單；完成後 `SaveCompletedSourceSets()` 卻使用本次擷取的「新設定來源集合」作為已完成簽章。既有 task 實際掃描的是變更前的舊來源。
- 影響：使用者新增或切換設定路徑時，新路徑中的卡片可能完全不出現；由於 `_loadedSourceSets` 已被錯誤更新，後續朋友來源事件也可能判定不需重載，直到另一次強制掃描才恢復。
- 修正：設定來源變更現在進入與朋友來源相同的序列化重載佇列，等待舊 task 後重新擷取差異；若分類正被其他流程載入，強制重載會等該 task 結束後再執行自己的掃描。只等待既有 task 的非強制分支不再提交來源簽章。
- 驗證：2026-07-31 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 123/123 通過。仍需讓舊根目錄載入停在中途再切換到含唯一卡片的新根目錄，確認新卡出現、舊卡移除，且簽章只在新掃描成功後更新。

## FRI-041：檔案監看器會漏掉仍在寫入的新卡與既有卡更新

- 嚴重度：高
- 狀態：程式修復完成，慢速寫入整合驗證待補
- 證據：`CardScanService.StartWatching()` 的 `NotifyFilter` 包含 `LastWrite`，卻只訂閱 `Created`、`Deleted`、`Renamed`，沒有訂閱 `Changed`。`Created` 經固定 300 ms 後只嘗試解析一次；若大型 PNG 或網路磁碟複製尚未完成，`TryCreateCard()` 失敗後不會再重試。直接覆寫既有 PNG 時也沒有任何處理。
- 影響：使用者把卡片放進朋友專用資料夾後可能看不到；同一路徑的角色或場景更新也可能繼續顯示舊縮圖、舊時間與舊分類，直到完整重載。
- 修正：watcher 已訂閱 `Changed`，每次寫入都會重啟 300 ms debounce；解析失敗且檔案仍存在時最多共嘗試四次。同一路徑解析成功時，三種收藏庫 ViewModel 會原位替換舊卡並重新排入 metadata，而非忽略更新。
- 驗證：2026-07-31 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 123/123 通過。仍需執行慢速分段寫入、覆寫既有 PNG 與連續快速寫入整合測試，確認集合只保留最新卡及縮圖／分類更新。

## FRI-042：已刪除或已被替換的角色卡可能被背景解析重新加入版本索引

- 嚴重度：中
- 狀態：程式修復完成，延遲解析整合驗證待補
- 證據：`CharacterGalleryViewModel.ParseMetadataAsync()` 完成後無條件在 UI 執行緒呼叫 `ApplyMetadata(card, meta)` 與 `UpdateVersionIndex(card)`；`OnCardRemoved()` 雖會先從 `_versionIndex` 移除該物件，卻不會取消單一卡片的既有解析。FRI-041 加入同路徑替換後也會產生同一競態。
- 影響：卡片已從 `Cards` 與 `_cardIndex` 消失，舊解析仍可把物件放回 `_versionIndex`，造成角色版本視窗顯示已刪除檔案、錯誤版本數或錯誤的最新版本。
- 修正：套用背景解析結果前會確認 `_cardIndex[filePath]` 仍是同一個物件；已刪除或替換的結果直接丟棄。替換同路徑角色卡會先從版本索引移除舊物件，再放入新物件並重新解析 metadata。
- 驗證：2026-07-31 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 123/123 通過。仍需阻塞舊卡解析，期間刪除或覆寫後再釋放，確認版本索引不會重新出現舊物件。

## FRI-043：設定卡片根目錄儲存失敗時仍會改變本次執行狀態

- 嚴重度：高
- 狀態：已修復；故障注入整合驗證待補
- 證據：新增／移除場景、角色、服裝根目錄時，命令先修改 `ObservableCollection` 並觸發來源重載，再呼叫私有 `SaveConfigAsync()`；該方法捕捉例外後只寫日誌、不向呼叫端回報，也不回復集合。
- 影響：磁碟滿、權限或 I/O 失敗時，畫面與 watcher 會使用未持久化的新路徑，使用者卻看不到失敗；本次執行可在該路徑建立朋友資料夾，重新啟動後設定消失，形成難以理解的孤立資料夾與卡片。
- 修正：三類根目錄新增／移除會暫時抑制集合的來源通知，使用會回拋例外的設定儲存；失敗時依原索引回復集合且不通知來源，成功後才精準觸發一次對應來源變更。設定頁六個入口共用 `UiEventGuard`，失敗會顯示可見的三語系 `InfoBar`；相同路徑也改為不分大小寫去重。
- 驗證：2026-08-01 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 123/123 通過，`git diff --check` 通過。仍需分別對三類路徑的新增與移除注入儲存失敗，確認集合、watcher、來源簽章與磁碟設定都維持操作前狀態，且重啟後一致。

## FRI-044：舊卡縮圖工作可能覆蓋同路徑新版卡片的快取

- 嚴重度：高
- 狀態：已修復；延遲縮圖整合驗證待補
- 證據：`ThumbnailRequestScope` 只以檔案路徑去重，舊卡縮圖尚在產生時，同路徑新卡的請求會被拒絕。舊工作完成後，`ThumbnailService` 又只以路徑呼叫 `SceneCardCacheService.SetThumbnailPath()`；該方法不核對快取項目的檔案大小或修改時間，會把舊縮圖路徑寫進已由 watcher 替換成新版的場景快取項目。
- 影響：朋友資料夾中的場景卡或角色／服裝卡原地覆寫後，畫面可能持續顯示舊縮圖；場景快取還會把錯誤路徑持久化，重啟後仍可能沿用。新版卡片因請求被同路徑舊工作壓掉，也不一定會立即重新產生縮圖。
- 修正：`ThumbnailRequestController` 以路徑、檔案大小與修改時間組成請求鍵，同路徑新快照可與舊工作分開排程；`SceneCardCacheService.SetThumbnailPath()` 使用原子條件更新，只有目前快取項目的大小與時間仍等於要求快照才接受縮圖。縮圖服務也會在讀取前與發佈暫存 JPEG 前再次核對來源快照，變更中的檔案不會發布半舊結果。
- 驗證：2026-08-01 新增同路徑不同快照可並行請求的核心測試；核心測試 124/124 通過，最新 Debug x64 建置 0 錯誤、0 警告。仍需阻塞舊卡縮圖產生，覆寫同路徑卡片並啟動新請求後再釋放舊工作；畫面與 `scene_cards.json` 最後都只能指向新版縮圖。

## FRI-045：卡片版本與快取鍵只看修改時間，保留時間戳的覆寫會沿用舊資料

- 嚴重度：中
- 狀態：部分修復；等長且保留時間戳的跨重啟辨識待決策
- 證據：場景收藏庫從持久化快取與實際掃描結果對帳時，只比較 `DateModified.Ticks` 決定同路徑卡片是否更新；三分類共用的 `ThumbnailCacheService` 與各分類 `MetadataCacheService`，SHA-256 輸入也只有 `filePath|dateModified.Ticks`。`CardBase` 已保存 `FileSize`，但上述版本判定與快取鍵均未使用。
- 影響：備份、同步或複製工具若保留原修改時間，內容或檔案大小已改變的朋友場景卡可能在完整重載後仍保留舊卡物件；三種卡片即使重新建立物件，也可能套用舊縮圖與舊遊戲／角色／服裝 metadata。
- 已完成：場景掃描對帳、縮圖快取鍵、metadata 快取鍵與縮圖來源核對均加入 `FileSize`；保留修改時間但大小不同的卡片不再沿用舊快取。最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。
- 尚未完成：大小與修改時間都相同但內容不同時，跨重啟後仍沒有可靠區別。完整內容雜湊會讓每次啟動重讀所有大型卡片，可能重新造成使用者回報的卡頓；較低成本的頭尾取樣指紋則不是完全保證，需決定效能與準確性取捨。
- 驗證：先以相同路徑、相同修改時間但不同大小的有效卡片覆寫，確認三分類產生新縮圖／metadata；選定內容指紋方案後，再測試相同大小與相同時間的內容替換及大型收藏庫載入成本。

## FRI-046：設定來源路徑一次變更會由頁面與協調器重複載入

- 嚴重度：高
- 狀態：已修復；分類載入計數整合驗證待補
- 證據：`GalleryPage`、`CharacterGalleryPage`、`CoordinateGalleryPage` 各自訂閱對應的 `SettingsViewModel.*FolderPathsChanged`，啟用中會立刻呼叫 `LoadCardsCommand`；`AuthorSourceCoordinator.Initialize()` 也訂閱同一組事件，`OnFolderPathsChanged()` 會從三分類排入強制來源重載。若頁面掃描已開始，協調器的 `AwaitOrLoad(forceReload: true)` 會等待目前工作後再執行自己的第二輪掃描。
- 影響：新增或移除一個設定路徑會讓同一收藏庫連續清空／掃描兩次，並額外檢查其他兩類來源；大型收藏庫會出現明顯卡頓、載入環與重複刷新，也會放大朋友詳情頁收到 `CardsReloaded` 的次數。
- 修正：設定與朋友來源變更都由 `AuthorSourceCoordinator` 的同一序列佇列處理；三個頁面已移除自己的來源事件訂閱。協調器改用場景／角色／服裝三個明確處理器，只排入實際變更的一類；設定事件訂閱也移到作者外掛可用性檢查之前，因此沒有作者外掛時仍會更新收藏庫。
- 驗證：2026-08-01 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。仍需對三類各新增／移除一次路徑，以載入計數確認對應命令每次只執行一輪、其他兩類不載入，並覆蓋收藏庫頁啟用與未啟用狀態。

## FRI-047：刪除朋友時無法分類的手動連結可能留在收藏庫

- 嚴重度：中
- 狀態：已修復；遺失／改卡種連結整合驗證待補
- 證據：`DeleteAsync()` 只以 `GetLinkedCardSourceKinds(friend.CardPaths)` 計算要失效的分類；連結檔已刪除、無法讀取或剛由一種卡片覆寫成另一種時，`GetLinkedCardType()` 只能回傳 `NotACard` 或目前類型，舊分類不會包含在 `changedSources`。相較之下，單張 `RemoveCardAsync()` 在無法分類時會保守失效全部三類。
- 影響：根目錄外的手動連結本來就沒有 watcher；刪除朋友後，先前載入的遺失卡或舊類型卡物件可能繼續留在收藏庫與作者統計，直到另一次完整重載或重啟。
- 修正：朋友只要含任何手動連結，刪除時就保守失效場景、角色、服裝三類；單張解除連結同樣失效三類，不再依目前可讀到的卡種猜測舊集合。刪除與解除連結都是低頻操作，資料正確性優先於省下兩類掃描。
- 驗證：2026-08-01 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。仍需載入根目錄外手動連結後，分別刪除原檔、把檔案改成另一卡種，再刪除朋友；三個收藏庫及作者統計都不得留下該路徑。

## FRI-048：三個主收藏庫仍以卡片數量判斷是否需要載入

- 嚴重度：中
- 狀態：已修復；空集合與失敗回復導覽驗證待補
- 證據：`GalleryPage`、`CharacterGalleryPage` 與 `CoordinateGalleryPage.OnNavigatedTo()` 都以 `ViewModel.Cards.Count == 0 || _reloadPending` 啟動掃描，沒有使用收藏庫已提供的 `HasCompletedLoad`。朋友詳情頁在 FRI-018 已改正，但主收藏庫頁面仍保留舊判斷。
- 影響：合法為空的分類每次進入頁面都會重新掃描，呈現重複載入與卡頓；相反地，載入取消或失敗後若 FRI-035 回復了非空舊快照，`Cards.Count > 0` 會讓頁面誤以為成功，不再重試新來源。
- 修正：三頁統一只在 `!HasCompletedLoad` 時要求載入；設定與朋友來源變更由 FRI-046 的協調器負責，頁面私有 `_reloadPending` 與只為它存在的啟用狀態已移除。
- 驗證：2026-08-01 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。仍需確認空分類成功載入後反覆進出不再掃描；新來源載入失敗並回復非空舊快照後，再進頁面會重試且僅在成功後把 `HasCompletedLoad` 設為真。

## FRI-049：同一路徑的相反檔案事件可能以不確定順序套用

- 嚴重度：高
- 狀態：已修復；快速替換 watcher 整合驗證待補
- 證據：`CardScanService` 把新增／變更保存為 `+完整路徑`、刪除保存為 `-完整路徑`，兩者可同時存在同一個 `HashSet<string>`；flush 依 `HashSet` 列舉順序逐一送出 `CardAdded`／`CardRemoved`。快速刪除重建或應用程式以暫存檔原子替換時，同一路徑會同時有正負事件，但 `HashSet` 不保證業務所需的事件先後。
- 影響：實體檔案最後存在時收藏庫可能仍移除卡片，最後不存在時也可能短暫或持續留下卡片；朋友詳情、作者統計與場景快取會跟著得到錯誤最終狀態。
- 修正：新增、變更、刪除與重新命名事件現在只把原始路徑加入同一個不分大小寫集合；flush 不再重播正負中間事件，而是重新檢查每個路徑此刻是否存在，存在就解析並新增／替換，不存在就移除。
- 驗證：2026-08-01 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。仍需在同一個 debounce 視窗內執行刪除→重建及變更→刪除，確認最後集合與磁碟一致且每個路徑只有一張卡。

## FRI-050：卡片覆寫後持續解析失敗會保留舊卡物件

- 嚴重度：高
- 狀態：已修復；損壞覆寫 watcher 整合驗證待補
- 證據：`FlushPendingChanges()` 對新增／變更路徑解析失敗時呼叫 `QueueParseRetry()`；檔案仍存在但達 `MaxParseAttempts` 後，該方法只移除重試計數並返回，從未送出 `CardRemoved`。同樣地，解析前檔案剛消失時也只清計數。
- 影響：原本有效的朋友卡若被覆寫成無效 PNG 或長時間維持截斷狀態，原分類會永久保留舊物件、舊縮圖與作者資料，直到完整重載。
- 修正：解析失敗會繼續既有的有限次重試；檔案消失或四次嘗試耗盡後會明確送出 `CardRemoved`。慢速寫入稍後若再產生 `Changed`，重試計數會重設，成功後可建立新卡並重新加入。
- 驗證：2026-08-01 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。仍需先載入有效卡，再原地覆寫成無效內容；舊分類在重試結束後必須移除，稍後寫回有效卡時能重新加入且不存在重複項目。

## FRI-051：朋友詳情不會收到 watcher 的單張卡片變更

- 嚴重度：高
- 狀態：已修復；三分類檔案操作 UI 驗證待補
- 證據：`FriendDetailPage` 只訂閱三個 ViewModel 的 `CardsReloaded`；此事件只在完整 `LoadCardsAsync()` 成功時觸發。`OnCardAdded()`／`OnCardRemoved()` 的 watcher 路徑只修改各自的 `Cards`，不會送出 `CardsReloaded`；場景新增雖送 `ViewRefreshed`，朋友頁也沒有訂閱，角色與服裝甚至沒有該通知。
- 影響：停留在朋友詳情時，把新卡放入專用資料夾、刪除卡片或原地覆寫新版，卡片格仍顯示舊集合與舊物件；必須切換分類、離開重進或完整重載才會同步。
- 修正：三個收藏庫提供只在 watcher 成功新增／替換／移除集合後觸發的 `CardsChanged`，與搜尋、篩選及 metadata 的 `ViewRefreshed` 分離。朋友詳情只接受目前分類事件，並把同一 dispatcher 批次中的多張卡通知合併成一次 `RefreshCards()`；離頁或切分類後的排程會再次檢查生命週期與分類。
- 驗證：2026-08-01 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。仍需停留在三個分類時依次建立、覆寫、刪除專用資料夾卡片，確認畫面為正確最終集合；一次複製多張卡時，合併後 UI 刷新次數不得等於每張卡一次。

## FRI-052：watcher 變更後收藏庫的空白狀態通知不完整

- 嚴重度：低
- 狀態：已修復；0→1／1→0 UI 驗證待補
- 證據：角色與服裝 ViewModel 的 `OnCardAdded()`／`OnCardRemoved()` 只修改 `Cards`，沒有通知 `IsEmpty`；場景新增呼叫 `RaiseViewRefreshed()` 間接通知，但 `OnCardRemoved()` 沒有。`IsEmpty` 是計算屬性，沒有集合變動的自動屬性通知。
- 影響：第一張卡由 watcher 加入後，空白提示可能仍覆蓋收藏庫；刪除最後一張卡時也可能沒有出現空白提示，直到其他篩選、導覽或完整載入觸發更新。
- 修正：`GalleryViewModelBase.RaiseCardsChanged()` 同時通知 `IsEmpty` 與集合事件；三分類只在 UI 集合成功新增／替換／移除後呼叫。場景移除也補上既有的 `ViewRefreshed`，讓可見縮圖與版面同步。
- 驗證：2026-08-01 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。仍需三分類分別從 0→1、1→0，確認空白提示立即且只更新一次；同路徑替換的 1→1 不得短暫顯示空白。

## FRI-053：記錄中的朋友資料夾遺失時仍顯示開啟按鈕

- 嚴重度：中
- 狀態：已修復；三分類遺失／重建 UI 驗證待補
- 證據：`FriendDetailPage.UpdateHeader()` 的 `hasFolder` 只判斷路徑字串非空，不呼叫 `Directory.Exists()`；因此實體目錄被刪除後，`OpenFolderButton` 仍顯示、`SetFolderButton` 反而隱藏。FRI-024 的同路徑重建在 `SetFolderAsync()` 內會回傳 `changed=false`，只送 `LibrarySourcesChanged`，不送 `FriendChanged(Folders)`。
- 影響：使用者看到的主要操作一定失敗，只能從更多選單猜測「變更資料夾」才能重建；即使重建成功，詳情標頭與按鈕也不會收到事件更新，可能繼續顯示錯誤狀態。
- 修正：標頭分開 `hasFolderPath` 與 `folderExists`；只有實體目錄存在時顯示開啟及角色子目錄按鈕，遺失時直接顯示設定按鈕、保留實際舊路徑及解除連結功能。開啟時才發現遺失也會立即重算標頭；同路徑重建會送出一次 `FriendChanged(Folders)`，再由既有事件流程同步清單與詳情。
- 驗證：2026-08-01 最新 Debug x64 建置 0 錯誤、0 警告，核心測試 124/124 通過。仍需保留記錄但刪除三類實體目錄，確認進詳情直接看到設定按鈕；以相同根重建後不切頁即可切回開啟按鈕，且只觸發一次標頭／卡片更新。

## FRI-054：來源切換掃描期間仍會接收舊 watcher 事件，且新 watcher 存在空窗

- 嚴重度：高
- 狀態：設計決策
- 證據：三個 `LoadCardsAsync()` 都先掃描及重建集合，直到結尾才呼叫 `CardScanService.StartWatching(paths)` 原子替換 watcher；因此整段掃描仍由上一組根目錄送出 `CardAdded`／`CardRemoved`。角色與服裝完整載入沒有場景的最終掃描索引對帳，舊事件可直接混入新集合；而新根目錄在列舉完成到 watcher 啟用之間沒有監看。
- 影響：移除或更換朋友／設定來源時，舊根剛好變動可能讓不再屬於來源的卡混回收藏庫；新根在監看空窗建立的卡可能直到下次完整重載才出現。朋友詳情與作者統計會忠實反映這個錯誤集合。
- 設計取捨：直接在掃描前停掉舊 watcher 會破壞 FRI-039 的失敗保留；掃描後再掃一次則讓大型收藏庫成本翻倍。較完整方案是 watcher 世代化：先建立新組並緩衝其事件，掃描成功後原子提交新世代、丟棄舊世代事件並重播新世代緩衝；掃描失敗只丟棄候選新組，舊組持續有效。
- 驗證：阻塞新根掃描，在期間同時修改舊根與新根；成功提交後集合只含新根最終狀態。再注入掃描失敗，確認舊集合與舊 watcher 事件完整保留、新世代事件不外洩。

## FRI-055：朋友來源背景重載失敗只寫日誌，前景仍顯示操作成功

- 嚴重度：中
- 狀態：設計決策
- 證據：`FriendService.SetFolderAsync()`／`ClearFolderAsync()`／卡片連結操作在資料成功持久化後同步送出 `LibrarySourcesChanged`，前景處理器隨即顯示成功；`AuthorSourceCoordinator.ReloadSourcesAsync()` 稍後才執行實際掃描與 watcher 切換，`catch` 只呼叫 `_logger.LogError()`，沒有事件或 UI 狀態回傳給發起頁面。
- 影響：資料夾記錄確實成功，但權限、I/O、watcher 資源或掃描例外會讓卡片集合維持舊快照；使用者只看到「專用資料夾已準備完成」卻看不到卡片，也無法分辨需要重試、修權限或重啟。
- 設計取捨：不能把資料持久化與可能耗時數十秒的索引掃描綁成同一交易；較合理的是協調器發布分類明確的載入失敗／恢復事件，當前朋友詳情或設定頁顯示「資料夾已保存，但收藏庫更新失敗」及重試按鈕，同時保留原成功事實。
- 驗證：注入三類掃描及 watcher 建立失敗；前景先確認資料已保存，再顯示對應分類的可見錯誤，按重試成功後錯誤消失且集合更新。離開發起頁時不得對已卸載控制項回呼。

## FRI-056：大型場景索引啟動期間主視窗長時無回應

- 嚴重度：高
- 狀態：已修復；已解除寫入型 UI 流程禁令
- 證據：2026-08-01 以正確 `bin/x64/Debug/.../win-x64` 輸出及 `winapp run` 啟動 PID 36364。安全化朋友 UI 腳本在第 1 項「`FriendsNavItem` 存在」即 fail-fast，未進入任何寫入。啟動超過 60 秒後程序仍 `Responding=False`，UI Automation 找到視窗但得到 0 個元素；5 秒樣本的 CPU 增量為 7.1094 秒，Working Set 約 689–722 MB。同一期間 9,205,904 bytes 的 `scene_cards.json` 於 12:49:32 被重寫，顯示問題與大型場景快取／掃描同時發生。
- 後續證據：自製 EventPipe 取樣器成功取得 3,060 個 managed 樣本；掃描 CPU 尖峰結束後，主執行緒樣本只能看到 WinUI 原生 `Application.Start`，無法從 managed 堆疊再往下定位。數分鐘後 UI Automation 可再讀到完整樹與 17,069 張場景卡，但點擊朋友導覽仍不生效；測試視窗最後以精確 Close 按鈕關閉，不影響其他程式。WPR 因系統「無法啟用 profile system performance policy」而無法取得 native/XAML trace，沒有繞過該政策。
- 啟動期取樣：第二次啟動 PID 87524 後立即取樣 30 秒，共得 366,415 個樣本。實際應用程式熱點包含 `PngHelper.ReadDimensions` 的平行檔案開啟／讀取，以及 UI 執行緒的 `AdvancedCollectionView.HandleSourceChanged -> Refresh -> BinarySearch/Compare`；當時快取還原每 200 張卡就結束一次 `DeferRefresh`，17,069 張會重複排序數十次。取樣結束後 PID 87524 為 `Responding=True`、Working Set 295.5 MB，`FriendsNavItem` 與 `GallerySearchBox` 均在 24 ms 內找到，證明問題集中在大型啟動載入階段，不是穩態無限迴圈。
- 測試限制生效：嘗試在 PID 87524 重跑會寫入 `friends.json` 的完整 UI 流程時，審核器依本條「修正前禁止」規則拒絕執行。沒有繞過、也沒有對朋友資料造成任何變更；完整流程必須等本項修正與只讀啟動驗證後才解禁。
- 影響：使用者在程式啟動後看到無法交互或黑色視窗，左側導覽與朋友頁都無法操作；也會讓 FRI-028 的黑畫面與朋友功能回歸得到誤判。
- 資料安全：失敗前後 `friends.json` SHA-256 均為 `DE8D248734292EA8B36F61E9FB4B85B02E827079824B44253F99F89F223BD315`；本次沒有新增、刪除或重命名朋友。腳本備份為 `artifacts/friends-ui-test-backup-20260801124845603.json`。
- 修正：主視窗啟用後才以 `DispatcherQueuePriority.Low` 啟動初始收藏庫工作，三個收藏庫頁的首次載入也改為低優先且重新檢查頁面與載入狀態。協調器依場景→角色→服裝串行等待，不再同時對三類根目錄進行大型掃描；每類掃描最多 4 個工作者。場景快取還原全程只使用一次 `AdvancedCollectionView.DeferRefresh`，每 200 張用 1 ms 計時器真正讓出 UI 回合；載入期間也不再為每張卡重送不會變的 `IsEmpty` 通知。
- 驗證：2026-08-01 以同一真實設定、17,069 張場景快取及正確 x64 Debug 輸出啟動 PID 78652；從程序 `StartTime` 到 `FriendsNavItem` 首次可找只需 1.660 秒。接下來 10 個每秒樣本全數 `Responding=True`；CPU 工作主要在前 6 秒完成，Working Set 從 238.2 MB 上升後穩定於約 295.6 MB。最新 x64 Debug 建置 0 錯誤、0 警告，核心測試 124/124 通過。讀取型啟動驗證已滿足解禁條件，可重跑安全化完整 UI 與穩定性流程。

## FRI-057：協調器等待頁面已開始的載入後不保存來源簽章

- 嚴重度：中
- 狀態：程式修復完成；來源事件整合驗證待補
- 證據：`AuthorSourceCoordinator.AwaitOrLoad()` 在 `isLoading && !forceReload` 時會把當前 `ExecutionTask` 加入等待清單，卻回傳 `false`；呼叫端只在回傳 `true` 時才把該類加入 `loadedKinds`。`SaveCompletedSourceSets()` 因此不會保存已等待成功的當前分類簽章。啟動時 `GalleryPage` 通常先開始場景載入，正好命中此分支。
- 影響：下一次場景設定或朋友來源事件到來時，`GetChangedSourceKinds()` 因 `_loadedSourceSets` 沒有記錄而必定將場景視為已變更，即使實際來源簽章相同也會多跑一輪大型掃描，再次帶來卡頓與刷新。
- 修正方向：請求的分類只要已成功完成、成功等待現有工作，或已有 `HasCompletedLoad`，都由 `SaveCompletedSourceSets()` 依「已完成且前後簽章相同」條件保存；不再把「是否新開工作」誤當成「是否可記錄簽章」。
- 修正：`AwaitOrLoadAsync()` 只負責成功等待或啟動當前分類；每一類完成後立即獨立呼叫 `SaveCompletedSourceSets()`，由它再檢查 `HasCompletedLoad` 與前後簽章。後續分類失敗時，已成功分類的簽章也不會一起遺失。最新 x64 Debug 建置 0 錯誤、0 警告，核心測試 124/124 通過。
- 驗證：先由收藏庫頁啟動場景載入，再呼叫協調器 `EnsureLoadedAsync()`；完成後送出相同來源事件不得重載，真正新增／移除來源時仍只重載對應分類。

## FRI-058：穩定性腳本沒有印出閒置 CPU 實際數值

- 嚴重度：低（測試基礎設施）
- 狀態：已修復
- 證據：2026-08-01 修正後 PID 78652 的 5 輪穩定性腳本 18/18 通過，但摘要顯示 `Idle sample: CPU +{0}s`。腳本把兩段字串以 `+` 連接後直接套 `-f`；PowerShell 運算子優先序使格式化只套到後一段，第一段的 `{0}` 被原樣保留。
- 影響：`$cpuDelta -gt 0.75` 的失敗門檻仍會正確執行，不會把過高 CPU 誤判為通過；但稽核記錄缺少實際數值，無法用來比較修正前後或小幅回歸。
- 修正：先以括號合併完整格式字串，再對整體套用 `-f`，CPU、Working Set 與畫面雜湊三個參數都會印出。
- 驗證：2026-08-01 以 1 輪導覽重跑，6/6 通過；摘要正確印出 `CPU +0s | Working Set -4.0 KB | Screenshots equal: True`，作者索引仍有資料；腳本最終通過 PowerShell AST parser，零語法錯誤。

## FRI-059：朋友角色卡的版本分類被錯誤建模成每個角色一組

- 嚴重度：高
- 狀態：已修復
- 使用者釐清：`#old_version` 與 `#alternatives` 是用來分類「某位朋友給予的舊卡與其他版本」，不是每個角色各自擁有一組；先前的「新增角色資料夾」功能源自需求理解錯誤。
- 證據：`CreateCharacterStructure()` 會建立 `朋友/角色名/#old_version` 與 `朋友/角色名/#alternatives`；詳情頁提供「新增角色資料夾」按鈕，`GetCharacterGroupPath()` 與版本索引也把角色資料夾當成群組邊界。
- 影響：介面要求使用者先建立角色資料夾，與預期的朋友層級整理方式相反。若只把保留資料夾上移而不改版本鍵，同一朋友的不同角色還會被錯誤合併成同一版本群組。
- 修正方向：角色卡專用路徑改為 `#Personal/朋友/#old_version` 與 `#Personal/朋友/#alternatives`；版本與替代卡以「朋友資料夾＋卡片角色名稱」分組。移除角色名稱輸入與建立角色資料夾流程，改成只在缺少時建立朋友層級的兩個分類資料夾；不自動搬移或刪除既有檔案。
- 延伸發現：替代卡現在必須等 metadata 的角色名稱解析完成後才能配對；若沿用原事件，詳情頁不會在替代卡解析或移除時更新，而且其他朋友的同名角色事件可能誤觸目前頁面。已新增以「朋友資料夾＋角色名稱」為範圍的替代卡通知，版本通知也加入朋友路徑隔離。
- 修正：新增朋友或設定角色卡專用路徑時，直接在朋友根目錄確保一組 `#old_version`／`#alternatives`；既有朋友缺少時顯示「建立版本分類資料夾」按鈕，不再詢問角色名稱。分類器只接受朋友根目錄的直接保留子目錄（其內可再巢狀）；舊的 `朋友/角色名/#old_version` 會視為一般目錄，程式不搬移或刪除內容。
- 自動驗證：新增朋友層級建立、冪等建立、缺少一側、巢狀分類、角色層級保留名稱忽略、不同角色與不同朋友隔離測試；核心測試 126/126 通過，正確 x64 Debug 建置 0 錯誤、0 警告。
- UI 驗證：PID 10840 的實際執行檔已核對為工作區最新 x64 AppX。YEAsukura 角色分類中，舊「新增角色資料夾」及角色名稱輸入均不存在；因根目錄已有 `#old_version` 但缺少 `#alternatives`，畫面正確顯示唯一的 `FriendCreateVariantFoldersButton`，可存取名稱為「建立朋友角色卡的版本分類資料夾」。既有根目錄舊版卡正確顯示「舊版本」徽章，按鈕及長文字沒有截斷或重疊；設定頁兩個核取方塊也明確寫成朋友的保留資料夾。為避免測試改動真實檔案，本輪沒有按下建立按鈕；建立與建立後冪等行為已由暫存目錄單元測試覆蓋。UI 前後 `friends.json` SHA-256 仍為 `DE8D248734292EA8B36F61E9FB4B85B02E827079824B44253F99F89F223BD315`，YEAsukura 的根目錄仍是 `#old_version=True`、`#alternatives=False`，測試程序已關閉。

## FRI-060：共用封裝 AUMID 可能讓 Computer Use 綁到舊安裝視窗

- 嚴重度：高（測試基礎設施）
- 狀態：已修復（驗證流程）
- 證據：FRI-059 第一次啟動後，`sky.list_apps()` 把同一視窗 ID 2107282 同時掛在開發封裝 AUMID 與舊版 `E:\KoikatsuSceneGallery-0.1.5-win-x64\KoikatsuSceneGallery.exe`；只依 AUMID 選窗後得到全黑擷取與無法點擊的元素幾何，卻沒有把視窗 HWND 與 `winapp run` 回傳 PID 的實際路徑交叉核對。
- 影響：自動化可能把舊安裝版或既有視窗的畫面、卡頓與功能誤算到工作區最新建置，造成錯誤回歸結論，甚至在錯誤版本上執行寫入型流程。
- 修正：啟動後先以 `Get-Process -Id <PID>` 核對實際 `Path` 位於預期 `bin/x64/Debug/.../win-x64/AppX`；關閉任何歧義視窗後重新列舉，僅在最新封裝回傳唯一新視窗時才互動。寫入型既有腳本仍以精確 PID `-a` 綁定，不改用 AUMID 猜測。
- 驗證：重新啟動 PID 10840 後，路徑精確指向工作區 AppX，新視窗 ID 8594924 的畫面正常且成功完成收藏庫→朋友→YEAsukura→角色→設定導覽；舊版視窗沒有參與結果，最後所有 `KoikatsuSceneGallery` 測試程序均已關閉。

## FRI-061：朋友清單需要獨立且不可刪除的本人檔案

- 嚴重度：無（新設計）
- 狀態：已修復
- 使用者需求：在所有朋友上方固定顯示一個屬於使用者自己的欄位；可修改顯示名稱與頭像、不可刪除，並作為新增及管理個人／作者卡片資料夾的實際範例。
- 設計風險：若把本人直接塞入 `FriendRecord` 集合並只加 `IsSelf`，搜尋、排序、重新命名、刪除、資料夾撞名與服務 API 都必須散布特殊分支；任何漏判都可能讓本人項目被隱藏、刪除或與同名朋友共用資料夾。
- 修正：建立獨立單例 `SelfProfile` 與 `self-profile.json`，不放進 `friends.json`；朋友頁在搜尋框及一般朋友清單之前顯示固定本人卡片，朋友搜尋與排序不影響它。本人可進入共用詳情視覺，以明確本人模式管理場景、角色與服裝卡片；服務層只有本人專用的改名、頭像、資料夾與卡片連結 API，沒有刪除本人 API，詳情頁也同時隱藏刪除項目並在處理器再次防守。
- 名稱與頭像：預設使用本地化名稱「我」及一般預設頭像，不自動讀取 Windows 帳號名稱或系統頭像。使用者可改顯示名稱、從本機選圖、連結已知線上作者頭像或清除頭像；畫面永久保留「本人／我的卡片」識別，不因顯示名稱改變而混同一般朋友。
- 資料夾：實體路徑固定使用各分類設定根目錄下的 `#Personal/#Self`，不跟隨可編輯顯示名稱改名；角色根目錄直接擁有一組 `#old_version` 與 `#alternatives`。一般朋友建立路徑時拒絕保留名稱，本人設定資料夾時也會檢查既有朋友是否已占用相同實體路徑。
- 進入規則：設定卡片根路徑時不建立 `#Self`。使用者點本人項目時才檢查；至少一類已綁定且存在即可進入，設定根目錄下已有 `#Self` 時只綁定既有目錄，多候選才讓使用者選擇，完全沒有時必須確認建立。「暫不建立」、沒有設定根路徑或所有候選皆衝突時不進入本人頁面；詳情切到缺少資料夾的分類也使用相同確認並在拒絕時還原原頁籤。
- 驗證：核心測試覆蓋獨立持久化、損壞欄位修復、交易失敗回復、固定本人資料夾與保留名稱；最新完整核心測試為 132/132。正確 x64 最新建置的唯讀 UI 流程 9/9 通過，確認本人永遠置頂、不被搜尋隱藏、可進入三分類、編輯操作存在而刪除操作不存在、閒置不刷新。測試前後 `friends.json` 雜湊相同，本人也沒有新增資料夾或卡片連結。

## FRI-062：本人置頂卡片的巢狀 x:Bind 被分析器判定可能在啟動時崩潰

- 嚴重度：中
- 狀態：已修復
- 證據：第一次以 Windows App SDK 分析器建置時，`FriendsPage.xaml` 對 `ViewModel.SelfProfile.Name`、頭像、資料夾與顯示狀態的六處巢狀 `x:Bind` 產生 WUI2010；分析器指出任何中間物件在啟動時為 null 都可能讓產生的綁定程式碼崩潰。
- 影響：即使目前 ViewModel 建構式會先建立本人清單項目，未來初始化順序或錯誤回復稍有變動，就可能讓整個朋友頁在開啟時失敗；也會讓新功能增加六個建置警告。
- 修正：`FriendsViewModel` 提供名稱、頭像、資料夾與可見狀態的扁平唯讀屬性；替換本人項目時明確通知這些屬性，XAML 不再穿越可能為 null 的中間物件。
- 驗證：重新啟用 Windows App SDK 分析器後，警告由初次的 54 項降為 48 項，`FriendsPage.xaml` 與 `FriendDetailPage.xaml` 都沒有 WUI2010／WUI2011；剩餘 48 項均位於其他既有頁面。最新 UI 流程 9/9 通過，置頂卡片及詳情都能讀取本人資料。

## FRI-063：本人 UI 測試把不會暴露成 UIA 元素的外層容器當成驗證目標

- 嚴重度：低（可存取性／測試基礎設施）
- 狀態：已修復
- 證據：唯讀測試呼叫 `SelfProfileItem` 後雖在 UIA 樹讀到 `FriendNameText=我`，仍因等待外層 `FriendCardTypeSelector` 而逾時；同樣地，設在 `Border` 上的 `SelfProfileBadge` AutomationId 也沒有出現在 UIA 樹。WinUI 的非控制型外層容器不一定建立 AutomationPeer。
- 影響：產品導航實際成功卻被測試誤報失敗；本人識別徽章若只靠外層 Border 的 ID，輔助技術也無法穩定定位。
- 修正：測試以實際可存取的 `FriendNameText` 與三個 `SelectorBarItem` 驗證詳情及分類；本人徽章 AutomationId 移到會建立文字 AutomationPeer 的內層 `TextBlock`。
- 驗證：最新 x64 建置的 UIA 流程可定位 `FriendNameText`、`SelfProfileBadge` 及三個分類項目，並完成 9/9；不再依賴 SelectorBar／Border 容器。

## FRI-064：本人保留路徑衝突檢查可能被尾端目錄分隔符繞過

- 嚴重度：中
- 狀態：已修復
- 證據：`EnsureSelfFolderIsAvailable()` 原本以 `StringComparison.OrdinalIgnoreCase` 直接比較既有朋友路徑與預期 `#Personal/#Self`；Windows 上 `C:\cards\#Personal\#Self` 與尾端多一個 `\` 的路徑是同一資料夾，但字串不相等。手動編輯或舊資料可能帶尾端分隔符。
- 影響：本人建立專用資料夾時可能未偵測到朋友已占用同一實體路徑，兩者會看到相同卡片，破壞本人與朋友的分類邊界。
- 修正：`FriendFolderLayout.AreSamePath()` 先取得完整路徑並移除尾端分隔符，再以 Windows 不分大小寫規則比較；本人路徑占用檢查改用此方法。
- 驗證：新增尾端分隔符相同、不同兄弟路徑不相同的回歸測試；核心測試 132/132 通過，Debug x64 建置 0 錯誤、0 警告。

## FRI-065：UI 測試經代理傳送空字串時會遺失必要參數

- 嚴重度：低（測試基礎設施）
- 狀態：已修復
- 證據：本人唯讀 UI 流程第一次在清除朋友搜尋框時呼叫 `set-value TextBox ''`；PowerShell 陣列雖包含空字串，經 `rtk proxy` 轉送後目標工具只收到 selector，回報「A value is required」。此前三項產品驗證均通過，因此不是搜尋或本人功能失敗。
- 影響：測試會在產品正常時誤報失敗並中止後續詳情、不可刪除與穩定性驗證。
- 修正：清除階段改寫入單一空白；`FriendsViewModel` 原本即以 `SearchText.Trim()` 判斷查詢，因此這在功能上等同空查詢，且不寫入任何持久化資料。
- 驗證：修正後從頭執行相同最新 x64 程序，9/9 全數通過；測試結束後朋友資料雜湊不變，本人資料夾與卡片連結仍為 0。

## FRI-066：沒有專用資料夾仍可直接進入本人頁面

- 嚴重度：中（流程／資料模型）
- 狀態：已修復
- 證據：`FriendsPage.SelfProfile_Click()` 原本無條件導航到 `FriendDetailPage`；詳情頁也只要求本人記錄存在，不檢查三種分類是否至少有一個實體資料夾。因此全新的本人記錄會進入沒有路徑、沒有專用卡片來源的空頁面。
- 影響：使用者選擇不建立資料夾後仍可能誤以為已完成設定；空頁面同時提供分類與管理操作，卻缺少此功能成立所需的檔案位置，流程語意互相矛盾。
- 修正：進入前改為延遲檢查。至少一個已綁定且存在的資料夾可直接進入；設定根目錄下已有未綁定的 `#Personal/#Self` 時只綁定、不建立或修改目錄；多個候選才要求選擇；完全沒有候選時一定顯示建立確認。按「暫不建立」、沒有設定根目錄或路徑衝突時留在朋友清單。本人詳情切到未設定的分類也採相同閘門，拒絕時還原原分類。
- 驗證：最新正確 x64 的無資料夾流程 6/6 通過，兩次點本人都顯示建立詢問，兩次「暫不建立」均留在朋友頁，沒有建立任何目錄且兩份 JSON 雜湊不變。受控既有場景 `#Self` 流程 4/4 通過：直接進入、只綁定場景、不建立其他分類；切到角色後拒絕建立會恢復場景選取。測試已逐位元組還原本人資料並刪除空測試目錄。

## FRI-067：同名實體資料夾會被靜默建立／沿用

- 嚴重度：高（資料邊界）
- 狀態：已修復
- 證據：`FriendFolderLayout.CreatePersonalFolder()` 與 `FriendService.SetFolderAsync()` 直接對清理後名稱呼叫 `Directory.CreateDirectory()`；Windows 會忽略大小寫沿用既有目錄。介面沒有先揭露實際路徑，也沒有確認該目錄是否屬於這份朋友檔案；服務層原本只防守本人 `#Self` 與同分類朋友的單一路徑情況。
- 影響：例如顯示名稱 `Alice` 與 `alice`，或兩個經非法字元清理後相同的名稱，可能在沒有確認下共用卡片；若同一實體路徑已被另一個分類或本人使用，還會破壞朋友之間的私人分類界線。
- 修正：新增忽略大小寫並以「清理後實體資料夾名」尋找既有目錄的預檢；未綁定的既有目錄必須經使用者確認才綁定，已屬於其他本人／朋友檔案則阻止操作。服務層跨三分類再次核對所有擁有者，避免 UI 遺漏或競態繞過。
- 驗證：核心測試增至 137 項，包含大小寫不同、`#Personal`／子目錄實際大小寫、名稱清理後撞名與 `#Self` 結構辨識。最新 x64 的已占用及未綁定既有目錄流程各 4/4 通過；占用時不寫入，未綁定時取消也不寫入或建立其他分類。確認綁定的服務路徑共用相同的忽略大小寫尋找結果，並由服務層跨擁有者再次核對。

## FRI-068：共用同一設定根目錄時只會失效第一種本人卡片來源

- 嚴重度：中
- 狀態：程式修復完成，待整合驗證
- 證據：本人首次批次建立三分類資料夾時，場景、角色與服裝可能選到同一個設定根目錄，因此三者的目標都是同一個 `#Personal/#Self`。初版迴圈在每次建立前才呼叫 `Directory.Exists()`；場景先建立後，角色與服裝便誤判成「原本已存在」，若三個欄位也早已記錄相同但遺失的路徑，就只會送出場景來源失效。
- 影響：重新建立遺失的共用 `#Self` 後，角色與服裝收藏庫可能仍沒有加入資料夾掃描／監看，直到其他重載或重啟才恢復。
- 修正：任何目錄建立前先為三分類各自保存實體存在狀態；批次完成後，所有在操作開始時目錄不存在的分類都精準送出來源事件，即使它們共用同一實體路徑。
- 驗證：待加入共用根目錄且三分類皆保留相同遺失路徑的服務／整合案例，確認一次建立會送出 Scene、Character、Coordinate 三種來源旗標。

## FRI-069：朋友 UI 測試把 JSON 屬性順序誤判成資料差異

- 嚴重度：中（測試基礎設施／資料安全）
- 狀態：已修復
- 證據：2026-08-01 最新 x64 完整流程前 20 項均通過，精確 GUID 的暫時朋友也已刪除；最後的「語意相同」檢查卻失敗。逐筆比對確認三筆 ID、名稱、資料夾、頭像與卡片連結完全相同，唯一差異是舊檔每筆以 `Id` 開頭，新序列化檔因繼承模型以衍生類別的 `FolderPath` 開頭。腳本目前以 `ConvertFrom-Json | ConvertTo-Json -Compress` 比較，仍保留屬性插入順序，因此不是真正的語意正規化。
- 影響：安全化測試會在產品資料沒有任何語意差異時誤報失敗；若直接依失敗訊息復原備份，還可能做出不必要的外部資料覆寫。
- 修正：將腳本的正規化改為遞迴排序 JSON 物件屬性、保留陣列順序後再比較；目前不把屬性順序差異視為朋友資料變更。測試前備份與序列化後版本都先保留，任何位元組復原仍需再次核對語意。
- 驗證：直接載入本次測試前後兩份檔案，修正版得到相同 canonical JSON；把第一筆名稱改為測試值時則正確偵測不同。修正後完整朋友 UI 流程 21/21 通過，所有 artifacts PowerShell 腳本均通過 AST parser。序列化後檔案另存證據後，真實 `friends.json` 已用當輪測試前備份逐位元組還原並核對 SHA-256。

## FRI-070：第二次 UI 測試的正常關閉超過十秒

- 嚴重度：低（效能／測試收尾）
- 狀態：已修復
- 證據：最新 x64 PID 37012 完成未綁定同名資料夾的唯讀流程後，`CloseMainWindow()` 回傳已接受，但 `WaitForExit(10000)` 逾時；未強制終止，緊接著的唯讀 `Get-Process -Id 37012` 已找不到程序，表示它最終自行結束，只是超過十秒門檻。第一次同一建置 PID 84616 則在相同十秒門檻內正常結束。
- 影響：使用者偶爾可能覺得應用程式關閉很慢；自動測試也可能把延遲收尾誤報成無法關閉，若測試框架直接強制終止還可能截斷尚未完成的安全寫入。
- 根因證據：`plugins.log` 在 18:40:13 明確記錄 `Fanbox WebView2 disposal timed out after 10 seconds with 3 active operation(s); forcing shutdown.`；這與正常關閉的十秒逾時一致。程序在外掛強制收尾後自行退出，`crash.log` 修改時間仍停在 2026-07-31 23:04:51，朋友及本人 JSON 雜湊也未變。
- 排查方向：Fanbox 外掛應在應用程式關閉前取消或完成 WebView2 活動操作，並讓 disposal 不必等待固定十秒；朋友功能不應為此加入強制終止或資料層例外處理。未修正外掛前，UI 測試維持正常關閉並允許超過十秒，不把延遲誤判成朋友功能失敗。
- 關鍵發現（2026-08-01）：Fanbox 的三筆作者查詢只連結收藏庫呼叫者的取消權杖，外掛沒有自己的關閉取消權杖；因此主視窗關閉後，查詢仍可停在 5／20 秒重試、40 秒導覽或 WebView2 script 等待，`FanboxWebViewHost.Dispose()` 只能等滿十秒再強制拆除。修正採獨立外掛生命週期權杖：operation drain 一進入 disposing 就先取消，作者查詢使用呼叫者與外掛權杖的 linked token，共享卡片 producer 仍不使用任何單一呼叫者的 token、只使用外掛關閉 token；這保留共享查詢隔離，同時允許程式關閉立即結束所有 WebView2 操作。新增離線測試鎖定「cancel → producer 結束 → host dispose → cache dispose」順序，Fanbox 測試 65/65 通過。
- 驗證（2026-08-01）：正確最新 Debug x64 AppX 已核對載入的 Fanbox DLL SHA-256 為 `D13FA9511FDA90EF5C1188EB908F1CBEA1E62051D979B82CF580244B9EF92715`。五輪都先等到 `Fanbox WebView2 initialized.` 後才以正常主視窗關閉，涵蓋朋友清單兩輪、本人建立詢問一輪、作者頁兩輪，退出時間依序為 187.8、188.3、249.0、164.4、198.5 ms，平均 197.6 ms、最慢 249.0 ms；另一次先行朋友頁樣本為 258.9 ms。所有輪次均未新增 `disposal timed out`、`forcing shutdown` 或 `deferring cache disposal`。`friends.json` 與 `self-profile.json` SHA-256 仍分別為 `DE8D248734292EA8B36F61E9FB4B85B02E827079824B44253F99F89F223BD315`、`ED2B095B77FABC6D91049C7BDCA5BEFAF21A2F66F0F5A32E34D9DD2A69486B5C`，`crash.log` SHA-256 維持 `9DB6D383FA0EDE1BFDBB386457DC357EBA0E0CBBC071A34A6320183723C42B71`。主程式核心測試 137/137、Fanbox 測試 65/65；一般 x64 Debug 建置 0 警告 0 錯誤，Windows App SDK 分析器建置仍只有其他既有頁面的 48 項警告。
- 驗證：至少五輪涵蓋朋友清單、本人建立詢問與作者頁的關閉時間；所有資料雜湊在關閉後維持一致，且沒有新增 crash.log。

## FRI-071：朋友卡片拖放流程與圖片比例顯示不完整

- 嚴重度：中（操作流程／版面）
- 狀態：程式修復完成，待跨視窗拖放人工整合驗證
- 證據：朋友詳情頁只有整頁的 `StorageItems` 拖入處理；朋友清單與置頂本人項目沒有 `AllowDrop` 或拖放事件，詳情的 `FriendCardsGrid` 也沒有 `CanDragItems`／`DragItemsStarting`。同一個卡片格把所有分類的容器固定為 220×292，雖然圖片使用 `Uniform`，橫式場景卡仍會在直式容器內留下大量空白。
- 影響：使用者不能從收藏庫直接拖到指定的朋友／本人，也不能把朋友頁中的連結卡片再拖到檔案總管或其他接受檔案的介面；場景、角色與服裝共用固定直式格子時，畫面密度和圖片可讀性都不穩定。
- 修正：新增共用 `CardDragDropHelper`，拖入只取 `StorageFile`、去除重複 PNG 路徑並用既有卡片分類器排除一般圖片；清單項目與本人項目接受拖入，成功後沿用交易式朋友服務新增連結。本人尚未設定資料夾時仍先走既有強制建立詢問，取消就不新增。詳情 `GridView` 可把卡片原始路徑包成要求 Copy 的 `StorageItems` 拖出。固定 220×292 容器已移除，版面會取目前卡片實際寬高比的中位數（空分類使用該卡種標準比例），配合縮圖尺寸設定與目前面板寬度重算欄寬／列高；圖片繼續使用 `Uniform`，不裁切內容。
- 驗證：核心測試 166/166，最新 x64 Debug 建置 0 錯誤 0 警告；三個語系 `.resw` 可解析，相關檔案 `git diff --check` 通過。實際最新 x64 程序進入 YEAsukura 詳情，場景分類呈現 1600×900 橫式卡片四欄，切到角色分類後重算為 756×1056 直式四欄，兩者圖片均完整顯示、檔名列正常且沒有裁切或固定直式空白。Windows 自動化工具無法可靠執行跨視窗拖曳，因此仍需人工確認「收藏庫／檔案總管拖到指定朋友或本人」及「詳情卡片拖到檔案總管」；程式碼層已確認拖入只呼叫連結 API，拖出要求 Copy，不含移動或刪除操作。
